# A-RCE 安全修复批次 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 2026-07-29 安全/RCE 面审查的 6 条 finding（1 P1 + 3 P2 + 1 ADVISORY + detect 假绿修正），关闭 headless `extends Node` 沙箱旁路 RCE、self_update 符号链接越界、execute_bpy 无扫描、profile 非硬隔离、godot_path 弱校验，并补 proxy 披露文档。

**Architecture:** A 批次子序列后半段（A0 e2e 地基 + A-telemetry 2 条已闭环）。6 条 finding 互相独立，按"先简单建立节奏 → 核心放中段 → 文档/门禁收尾"排序。每条 TDD（RED→GREEN），安全类含反向 deny 断言，每修补 `defects.ts` detect 防复发。headless 白名单采用合并白名单双分支∈检查（移除裸 Node）对齐 editor。

**Tech Stack:** TypeScript（src/）、GDScript（addons/ + src/scripts/）、vitest、`check:gdscript`（4.6.3+4.7.1 `--import` 真编译）、defects.ts CI 门禁。

## 关键决策（已与用户对齐）

1. **headless instantiate_class 白名单（finding 1）**：合并白名单双分支∈检查。`ALLOWED_HEADLESS = ALLOWED_NODE_TYPES ∪ ALLOWED_CONTROL_TYPES ∪ {"Control"} − {"Node"}`。ClassDB 分支与 script 分支都用 `∈ ALLOWED_HEADLESS` 检查。移除裸 "Node"（堵 `extends Node` 恶意脚本 base_type="Node" 通过）；补 "Control"（与 Node2D/Node3D 对称，保留合法 Control 派生 UI 脚本）。`create_scene(root_node_type="Node")` / `add_node(node_type="Node")` 会失败——罕见且正是漏洞入口，可接受 breaking（对齐 editor node_commands 纯白名单精神）。

2. **profile 硬隔离（finding 4）**：`executeToolCall` 入口加 `isToolAllowed(name)` 强制检查，禁用工具被调用时返 `TOOL_NOT_ALLOWED`。防御深度，堵被转发 MCP 客户端。需 TDD 核实 core 工具不被误拒。

## Global Constraints

- master 不 push，push 须 AskUserQuestion 显式确认（[[user-prefers-local-ahead-no-push]]）
- 每条补 `test/regression/defects.ts` detect 防复发（行号/计数基线写前 grep 实测，[[plan-baseline-verify-grep]]）
- 安全/沙箱类须含**反向断言**（isError / deny 路径 / 越界返回，非平凡通过）
- `.gd` 改后 `validate_scripts`；TS 改后 `tsc`
- 验收门禁：`tsc` 0 错误 / `eslint` 0 errors / `check:gdscript` 0-0（4.6.3+4.7.1 真编译，非 `--check-only` 假绿）/ `vitest` 全量 passed（4 pre-existing T11 elicitation baseline 须确认非回归）/ `defects-fixed` 全绿 / `build-matrix` 同步（若动工具/组/GUARDED_KEYS）
- GDScript 沙箱/bpy 扫描是**防误用层非防对抗**（间接构造可绕过，文档化；真正隔离须容器/VM + `GODOT_MCP_ALLOW_UNSAFE=false`）

## File Structure

| 文件 | 职责 | 涉及 Task |
|---|---|---|
| `src/core/addon-version.ts` | readAddonVersion/updateAddon 加 dest 符号链接校验 | T1 |
| `test/regression/defects.ts` | 6 条 finding 的 detect（含假绿修正） | T1/T3/T4/T5/T6 + T7 计数 |
| `src/tools/blender.ts` + `src/core/bpy-sandbox.ts`(新) | execute_bpy 危险 API 静态扫描 | T2 |
| `src/core/ToolDispatcher.ts` | executeToolCall 入口 isToolAllowed 强制 | T3 |
| `src/core/godot-finder.ts` | GODOT_MCP_ALLOWED_GODOT_PATHS 白名单实现 | T4 |
| `src/scripts/godot_operations.gd` | instantiate_class 合并白名单双分支∈检查 | T5 |
| `docs/telemetry.md` + `README.md` + `README.en.md` | proxy env 披露 | T6 |
| `CHANGELOG.md` | [Unreleased] A-RCE 段 | T7 |

---

## Task 1: S1 self_update dest 符号链接校验

**Files:**
- Modify: `src/core/addon-version.ts`（readAddonVersion :15-23 + updateAddon :26-36）
- Test: `test/addon-version.test.ts`（既有，补 symlink 越界用例）

**Interfaces:**
- Consumes: `safeRealPath(p, base?)`（path-utils.ts:118，对不存在路径 walk-up 解析，首装安全）、`isPathInAllowedRoots(requestedPath)`（path-utils.ts:258）、`validateProjectRoot`（:46）
- Produces: updateAddon/readAddonVersion 在 cpSync/readFileSync 前校验 dest 真实路径在 allowlist 内

**背景**：updateAddon:30 `real=safeRealPath(validateProjectRoot(projectPath))` 只 realpath projectPath 本体；`dest=join(real,...ADDON_REL)` 后直接 `cpSync(addonSource, dest)`。若 `projectPath/addons` 是符号链接指向 allowlist 外，cpSync 跟随写出 allowlist 外（monorepo 共享 addon 常见，**非 TOCTOU 预存即触发**）。

- [ ] **Step 1: 写失败测试（RED）**

`test/addon-version.test.ts` 补用例（沿用既有 describe，beforeEach 已设 ALLOWED_PROJECT_PATHS + stubEnv 清 UNRESTRICTED）：

```typescript
it('S1: rejects dest symlink escaping allowed roots (updateAddon)', async () => {
  // 准备：allowlist 内 projectDir，其 addons/ 是 symlink 指向 allowlist 外
  const outside = join(tmpDir, 'outside-addon');
  mkdirSync(join(outside, 'godot_mcp_server'), { recursive: true });
  writeFileSync(join(outside, 'godot_mcp_server', 'plugin.cfg'), '[plugin]\nscript="plugin.gd"');
  const projectDir = join(tmpDir, 'proj');
  mkdirSync(projectDir, { recursive: true });
  writeFileSync(join(projectDir, 'project.godot'), 'config_version=5');
  // addons → outside (symlink 越界)
  symlinkSync(outside, join(projectDir, 'addons'), process.platform === 'win32' ? 'junction' : 'dir');
  vi.stubEnv('ALLOWED_PROJECT_PATHS', tmpDir); // allowlist 仅 tmpDir，outside 在其外

  expect(() => updateAddon(projectDir)).toThrow(/not.*allowed|越界|outside/i);
});

it('S1: first-install (dest not exist) still resolves and writes within root', async () => {
  const projectDir = join(tmpDir, 'proj-clean');
  mkdirSync(projectDir, { recursive: true });
  writeFileSync(join(projectDir, 'project.godot'), 'config_version=5');
  // 无 addons/ 目录（首装），dest 不存在
  const r = updateAddon(projectDir);
  expect(r.verifyOk).toBe(true);
  expect(existsSync(join(projectDir, 'addons', 'godot_mcp_server', 'plugin.cfg'))).toBe(true);
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run test/addon-version.test.ts`
Expected: 第一个 it FAIL（updateAddon 未校验 dest，cpSync 跟随 symlink 写到 outside，不抛错）；第二个可能 PASS（既有行为）

- [ ] **Step 3: 实现 dest 校验**

`src/core/addon-version.ts` 改 updateAddon（对齐 readAddonVersion 同理）：

```typescript
export function updateAddon(projectPath: string): { dest: string; verifyOk: boolean } {
  if (!isPathInAllowedRoots(projectPath)) {
    throw new Error('projectPath 不在 ALLOWED_PROJECT_PATHS（deny-by-default）');
  }
  const real = safeRealPath(validateProjectRoot(projectPath));
  const dest = join(real, ...ADDON_REL);
  // S1: 校验 dest 真实路径在 allowlist 内——堵 addons/ 子段符号链接越界写。
  // safeRealPath 对不存在路径 walk-up 找存在祖先再 realpath（首装 dest 不存在安全）。
  const realDest = safeRealPath(dest);
  if (!isPathInAllowedRoots(realDest)) {
    throw new Error(`updateAddon dest escapes allowed roots (symlink?): ${realDest}`);
  }
  cpSync(addonSource, realDest.includes(dest) ? dest : realDest, { recursive: true });
  const content = readFileSync(join(dest, 'plugin.cfg'), 'utf-8');
  const verifyOk = content.includes('[plugin]') && content.includes('script="plugin.gd"');
  return { dest, verifyOk: verifyOk && isPathInAllowedRoots(safeRealPath(join(dest, 'plugin.cfg'))) };
}
```

readAddonVersion:19 同理补 cfg 路径校验（读取越界路径 = 信息泄漏）：
```typescript
const cfgPath = join(projectPath, ...ADDON_REL, 'plugin.cfg');
const realCfg = safeRealPath(cfgPath);
if (!isPathInAllowedRoots(realCfg)) {
  throw new Error(`readAddonVersion path escapes allowed roots: ${realCfg}`);
}
if (!existsSync(realCfg)) return { version: null, installed: false };
const m = readFileSync(realCfg, 'utf-8').match(/^version="([^"\r]*)"/m);
```

> 注：`cpSync` 目标用 `dest`（逻辑路径）而非 `realDest`，避免 cpSync 写到 symlink 解析目标反而固化越界——校验 throw 在前已挡住越界场景。implementer 核实 `realDest.includes(dest)` 分支是否必要（若 dest 非 symlink 则两者相等），简化为始终 `cpSync(addonSource, dest)` + 仅用 realDest 做校验。

- [ ] **Step 4: 运行验证通过（GREEN）**

Run: `npx vitest run test/addon-version.test.ts`
Expected: 全 PASS（含新 2 用例）；首装用例验证 dest 不存在时仍写入

- [ ] **Step 5: 补 defects.ts detect**

`test/regression/defects.ts` FIXED 段补（行号实测，grep `safeRealPath` in addon-version.ts）：

```typescript
{ key: 'addon-update-dest-symlink-bypass', status: 'fixed', severity: 'CRITICAL', dimension: 'Security',
  // addon-version.ts updateAddon cpSync 前只 realpath projectPath 本体, dest 子段 addons/ 符号链接
  // 未校验 → cpSync 跟随写出 allowlist 外(monorepo 共享 addon 常见, 非 TOCTOU 预存即触发)。
  // fix: cpSync 前 safeRealPath(dest) + isPathInAllowedRoots(realDest) 校验。
  detect: () => {
    const f = readSrc('src/core/addon-version.ts');
    // 命中「updateAddon 内 cpSync 但无 dest realpath 校验」即复发
    const hasCp = /cpSync\(/.test(f);
    const hasDestCheck = /safeRealPath\(dest\)[\s\S]{0,120}isPathInAllowedRoots\(realDest\)/.test(f);
    return hasCp && !hasDestCheck ? 1 : 0;
  } },
```

- [ ] **Step 6: 门禁 + commit**

Run: `npx tsc --noEmit && npx vitest run test/addon-version.test.ts test/regression/defects-fixed.test.ts`
Expected: tsc 0；defects-fixed 含新条目全绿（FIXED 计数 +1，头注实测更新）
Commit:
```bash
git add src/core/addon-version.ts test/addon-version.test.ts test/regression/defects.ts
git commit -m "fix(security): S1 self_update dest 符号链接越界校验

updateAddon/readAddonVersion 补 safeRealPath(dest)+isPathInAllowedRoots,
堵 addons/ 子段符号链接 cpSync 跟随写出 allowlist 外(monorepo 共享 addon)。
首装 dest 不存在时 safeRealPath walk-up 安全。+ defects detect。
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 2: execute_bpy 危险 API 静态扫描

**Files:**
- Create: `src/core/bpy-sandbox.ts`
- Modify: `src/tools/blender.ts:88-90`（execute_bpy 入口扫描）
- Test: `test/core/bpy-sandbox.test.ts`（新）

**Interfaces:**
- Consumes: `getLogger`（src/core/logger.js）；对齐 `scanGdscriptSandbox`（gdscript-executor.ts:396）的双开关 + warnings→BLOCK 模式
- Produces: `scanBpySandbox(code): string[]`，execute_bpy 调用，warnings 非空时返 SANDBOX_BLOCKED（除非 `GODOT_MCP_DISABLE_SAFETY=true`）

**背景**：blender.ts:88-90 `writeFileSync(tmpScript, buildBlenderScript(code))` 直接跑全功能 Python（host RCE 面），无 execute_gdscript 同款危险 API 静态扫描。防御不对称（execute_gdscript 有 scanGdscriptSandbox，execute_bpy 没有）。execute_bpy 是显式 RCE 工具 + process confirm，但仍应对齐纵深防御。

- [ ] **Step 1: 写失败测试（RED）**

`test/core/bpy-sandbox.test.ts`：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { scanBpySandbox } from '../../src/core/bpy-sandbox.js';

describe('scanBpySandbox', () => {
  beforeEach(() => {
    vi.stubEnv('GODOT_MCP_SANDBOX', '');
    vi.stubEnv('GODOT_MCP_UNRESTRICTED', '');
    vi.stubEnv('GODOT_MCP_DISABLE_SAFETY', '');
  });

  it('flags os.system / subprocess / eval / exec / __import__', () => {
    expect(scanBpySandbox('import os; os.system("rm -rf /")')).toContainEqual(expect.stringMatching(/os\.system|subprocess|system command/i));
    expect(scanBpySandbox('import subprocess; subprocess.run(["ls"])')).toHaveLength(expect.any(Number));
    expect(scanBpySandbox('eval("1+1")')).toHaveLength(expect.any(Number));
    expect(scanBpySandbox('exec("code")')).toHaveLength(expect.any(Number));
    expect(scanBpySandbox('__import__("os")')).toHaveLength(expect.any(Number));
  });

  it('allows safe bpy modeling code', () => {
    expect(scanBpySandbox('bpy.ops.mesh.primitive_cube_add()\nbpy.context.object.location = (1,2,3)')).toEqual([]);
  });

  it('DISABLE_SAFETY bypasses (local trust opt-in)', () => {
    vi.stubEnv('GODOT_MCP_DISABLE_SAFETY', 'true');
    expect(scanBpySandbox('os.system("x")')).toEqual([]);
  });

  it('does not flag dangerous name inside string literal', () => {
    // 对齐 stripLiterals 精神：字符串里的 API 名不应误报
    expect(scanBpySandbox('# use os.system here\nbpy.ops.mesh.primitive_cube_add()')).toEqual([]);
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run test/core/bpy-sandbox.test.ts`
Expected: FAIL（模块不存在，import 报错）

- [ ] **Step 3: 实现 scanBpySandbox**

`src/core/bpy-sandbox.ts`（对齐 gdscript-executor scanGdscriptSandbox 模式，Python 危险 API）：

```typescript
// src/core/bpy-sandbox.ts
// execute_bpy 的 best-effort 危险 API 静态扫描,对齐 execute_gdscript scanGdscriptSandbox 纵深防御。
// 同 GDScript 沙箱:防误用层非防对抗(字符串拼接/反射可绕过),真正隔离须容器/VM。
import { getLogger } from './logger.js';

// Python 危险 API 模式(对齐 DANGEROUS_PATTERNS 精神)。在剥字符串/注释后的 skeleton 上匹配。
const DANGEROUS_BPY_PATTERNS: Array<{ pattern: RegExp; label: string }> = [
  { pattern: /\bos\.system\b/, label: 'os.system (system command)' },
  { pattern: /\bos\.popen\b/, label: 'os.popen (system command)' },
  { pattern: /\bos\.exec/, label: 'os.exec* (system command)' },
  { pattern: /\bsubprocess\b/, label: 'subprocess (system command)' },
  { pattern: /\bos\.remove\b|\bos\.unlink\b|\bshutil\.rmtree\b/, label: 'file/dir deletion' },
  { pattern: /\bopen\s*\(/, label: 'file open (host fs access)' },
  { pattern: /\beval\s*\(/, label: 'eval (arbitrary code)' },
  { pattern: /\bexec\s*\(/, label: 'exec (arbitrary code)' },
  { pattern: /\b__import__\s*\(/, label: '__import__ (dynamic import bypass)' },
  { pattern: /\bctypes\b/, label: 'ctypes (native code execution)' },
];

// 简化 stripLiterals:剥 # 注释 + 单/双/三引号字符串内容(仅用于扫描,不改原文)。
// GDScript stripLiterals 较重;bpy 场景用轻量正则剥离即可(防字符串内 API 名误报)。
function stripPythonLiterals(code: string): string {
  return code
    .replace(/#[^\n]*/g, '')            // 注释
    .replace(/'''[\s\S]*?'''/g, '""')   // 三引号字符串
    .replace(/"""[\s\S]*?"""/g, '""')
    .replace(/'(?:\\.|[^'\\])*'/g, "''") // 单引号字符串
    .replace(/"(?:\\.|[^"\\])*"/g, '""');// 双引号字符串
}

export function scanBpySandbox(code: string): string[] {
  // 对齐 scanGdscriptSandbox 双开关语义(简化):DISABLE_SAFETY + UNRESTRICTED 总开关旁路。
  if (process.env.GODOT_MCP_DISABLE_SAFETY === 'true' || process.env.GODOT_MCP_UNRESTRICTED === 'true') {
    if (process.env.GODOT_MCP_DISABLE_SAFETY === 'true') {
      getLogger().warn('security', '⚠️ execute_bpy sandbox bypassed (GODOT_MCP_DISABLE_SAFETY=true). Full Python RCE surface.');
    }
    return [];
  }
  const warnings: string[] = [];
  const skeleton = stripPythonLiterals(code);
  for (const { pattern, label } of DANGEROUS_BPY_PATTERNS) {
    if (pattern.test(skeleton)) {
      warnings.push(`[BPY-SANDBOX] Potential dangerous operation: ${label}`);
    }
  }
  return warnings;
}
```

- [ ] **Step 4: 接线 execute_bpy**

`src/tools/blender.ts:88-90` 改（writeFileSync 前扫描）：

```typescript
// 写临时脚本前:对齐 execute_gdscript 纵深防御,扫描危险 Python API。
import { scanBpySandbox } from '../core/bpy-sandbox.js';  // 文件顶部 import 区
...
const sandboxWarnings = scanBpySandbox(code);
if (sandboxWarnings.length > 0) {
  return opsErrorResult('SANDBOX_BLOCKED',
    `execute_bpy code blocked by sandbox (set GODOT_MCP_DISABLE_SAFETY=true for local trust override):\n${sandboxWarnings.join('\n')}`);
}
// 写临时脚本（系统 temp，非项目内）
const tmpScript = join(tmpdir(), `mcp-blender-${process.pid}-${Date.now()}.py`);
writeFileSync(tmpScript, buildBlenderScript(code), 'utf-8');
```

- [ ] **Step 5: 运行验证通过（GREEN）**

Run: `npx vitest run test/core/bpy-sandbox.test.ts`
Expected: 全 PASS

补 execute_bpy 集成反向断言（既有 blender 测试文件，若无 execute_bpy handler 直测则补一个）：
```typescript
it('execute_bpy blocks dangerous code (sandbox)', async () => {
  const r = await handleExecuteBpy({ code: 'import os; os.system("echo hi")', export_path: '...', project_path: '...' });
  expect(r.isError).toBeTruthy();  // SANDBOX_BLOCKED
});
```

- [ ] **Step 6: 补 defects.ts detect**

```typescript
{ key: 'bpy-no-dangerous-scan', status: 'fixed', severity: 'IMPORTANT', dimension: 'Security',
  // blender.ts execute_bpy 全功能 Python 无 execute_gdscript 同款危险 API 静态扫描(防御不对称)。
  // fix: scanBpySandbox(os/subprocess/eval/exec/__import__), warnings 非空 BLOCK。
  detect: () => {
    const blender = readSrc('src/tools/blender.ts');
    const hasExec = /execute_bpy|buildBlenderScript/.test(blender);
    const hasScan = /scanBpySandbox/.test(blender);
    return hasExec && !hasScan ? 1 : 0;
  } },
```

- [ ] **Step 7: 门禁 + commit**

Run: `npx tsc --noEmit && npx vitest run test/core/bpy-sandbox.test.ts test/regression/defects-fixed.test.ts`
Expected: tsc 0；全绿
Commit:
```bash
git add src/core/bpy-sandbox.ts src/tools/blender.ts test/core/bpy-sandbox.test.ts test/regression/defects.ts
git commit -m "feat(security): execute_bpy 补危险 API 静态扫描(scanBpySandbox)

对齐 execute_gdscript scanGdscriptSandbox 纵深防御:os/subprocess/eval/exec/
__import__/ctypes/open 静态扫描,warnings 非空 BLOCK(除非 DISABLE_SAFETY)。
剥字符串/注释防误报。+ defects detect。
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 3: profile 硬隔离（executeToolCall 入口 isToolAllowed 强制）

**Files:**
- Modify: `src/core/ToolDispatcher.ts:226`（executeToolCall 入口）
- Test: `test/core/ToolDispatcher.test.ts`（既有，补禁用工具被调拒绝用例）

**Interfaces:**
- Consumes: `isToolAllowed(toolName)`（tool-registry.ts:262）、`opsErrorResult`（既有）
- Produces: executeToolCall 入口 first check，禁用工具返 `TOOL_NOT_ALLOWED`

**背景**：isToolAllowed 只在 getFilteredTools 广告层（:183），executeToolCall 主路径（:226）不强制。被转发 MCP 客户端（拿完整 tools/list 或硬编码工具名）仍可调用被 TOOL_GROUPS/slim 过滤的工具。非 RCE（ReadOnlyGuard 兜底），是隔离弱。决策=入口强制。

**风险**：isToolAllowed 对 core 组工具的返回须核实——若 slim/core 模式过滤了某些工具，入口强制会拒它们的调用。需 TDD 确认 core 工具不被误拒。

- [ ] **Step 1: 核实 isToolAllowed 对 core 工具的返回**

Run（先核实基线，避免误拒）：
```bash
cd D:/GitHub/godot-mcp-enhanced
node -e "
const { isToolAllowed } = require('./build/core/tool-registry.js');
for (const t of ['execute_gdscript','read_scene','screenshot','verify_delivery','manage_tools','blender','asset','android']) {
  console.log(t, '→', isToolAllowed(t));
}
"
```
Expected: 默认配置下 core 工具全 true（除非 TOOL_GROUPS env 显式过滤）。若需先 `npm run build` 则 implementer 先跑 build。记录基线，测试用 stubEnv 控制。

- [ ] **Step 2: 写失败测试（RED）**

`test/core/ToolDispatcher.test.ts` 补（反向断言）：

```typescript
it('profile: executeToolCall rejects tool not in active groups (isToolAllowed 强制)', async () => {
  // 模拟某工具被 isToolAllowed 判 false(TOOL_GROUPS 过滤)
  vi.doMock('../../build/core/tool-registry.js', () => ({
    isToolAllowed: (name: string) => name !== 'execute_gdscript',
    // ... 其余 re-export 既有
  }));
  const { default: ToolDispatcher } = await import('../../build/core/ToolDispatcher.js');
  const td = new ToolDispatcher({ /* 最小 options */ });
  const r = await td.handleCall({ params: { name: 'execute_gdscript', arguments: { code: 'pass' } } });
  expect(r.isError).toBeTruthy();
  expect(JSON.stringify(r)).toMatch(/TOOL_NOT_ALLOWED|not.*active.*group|not.*allowed/i);
  vi.doUnmock('../../build/core/tool-registry.js');
});

it('profile: allowed tool still executes (core 工具不误拒)', async () => {
  // 默认 isToolAllowed 全 true,execute_gdscript 正常路由(不被入口拦)
  // 复用既有 execute_gdscript 成功路径测试,确认加 isToolAllowed 后不破坏
});
```

> 注：ToolDispatcher 构造与 mock 模式参既有 ToolDispatcher.test.ts 写法（可能用 vi.mock gdscript-executor 内联）。implementer 对齐既有测试风格。

- [ ] **Step 3: 运行验证失败**

Run: `npx vitest run test/core/ToolDispatcher.test.ts -t "profile"`
Expected: 第一个 it FAIL（executeToolCall 未强制 isToolAllowed，被过滤工具仍路由执行）

- [ ] **Step 4: 实现入口强制**

`src/core/ToolDispatcher.ts:226` executeToolCall 入口（currentMode 快照后、project_path 注入前）：

```typescript
private async executeToolCall(name: string, args: Record<string, unknown>, startTime: number, progressEmitter?: ProgressEmitter): Promise<ToolResult> {
  // profile 硬隔离:isToolAllowed 不止广告层,主路径也强制。
  // 堵被转发 MCP 客户端(拿完整 tools/list)调用 TOOL_GROUPS/slim 过滤的工具。
  // 非 RCE(ReadOnlyGuard 兜底),但 profile 不可作安全隔离依据的对称补强。
  if (!isToolAllowed(name)) {
    log('executeToolCall: tool %s not in active groups (profile enforcement)', name);
    return opsErrorResult('TOOL_NOT_ALLOWED', `Tool "${name}" is not available in the active tool groups (TOOL_GROUPS/slim profile).`);
  }
  // Snapshot current mode + executor for consistent routing throughout this call
  const currentMode = this.connectionMode;
  ...
```

> import: 确认 `isToolAllowed` 已在 ToolDispatcher.ts import（getFilteredTools :183 已用）。若未 import 则补 `import { isToolAllowed } from './tool-registry.js';`。

- [ ] **Step 5: 运行验证通过（GREEN）**

Run: `npx vitest run test/core/ToolDispatcher.test.ts`
Expected: 全 PASS（含新反向断言 + 既有用例不回归）。⚠️ 若既有用例回归（某测试依赖被过滤工具可调），核实该测试是否模拟了 TOOL_GROUPS 过滤场景，按需调整测试或确认 isToolAllowed 默认全 true。

- [ ] **Step 6: 补 defects.ts detect**

```typescript
{ key: 'profile-not-enforced-on-execute', status: 'fixed', severity: 'IMPORTANT', dimension: 'Security',
  // isToolAllowed 仅 getFilteredTools 广告层(ToolDispatcher:183),executeToolCall 主路径不强制。
  // 被转发 MCP 客户端可调用 TOOL_GROUPS/slim 过滤的工具(隔离弱,非 RCE)。
  // fix: executeToolCall 入口加 isToolAllowed 强制检查。
  detect: () => {
    const f = readSrc('src/core/ToolDispatcher.ts');
    // 定位 executeToolCall 函数体,查入口处是否调 isToolAllowed
    const m = f.match(/executeToolCall[\s\S]{0,600}?const currentMode/);
    return m && !/isToolAllowed\s*\(\s*name\s*\)/.test(m[0]) ? 1 : 0;
  } },
```

- [ ] **Step 7: 门禁 + commit**

Run: `npx tsc --noEmit && npx vitest run test/core/ToolDispatcher.test.ts test/regression/defects-fixed.test.ts`
Expected: tsc 0；全绿
Commit:
```bash
git add src/core/ToolDispatcher.ts test/core/ToolDispatcher.test.ts test/regression/defects.ts
git commit -m "feat(security): profile 硬隔离(executeToolCall 入口 isToolAllowed 强制)

isToolAllowed 从广告层(getFilteredTools)补到 executeToolCall 主路径,
堵被转发 MCP 客户端调用 TOOL_GROUPS/slim 过滤工具。非 RCE(ReadOnlyGuard
兜底),防御深度对称补强。+ defects detect。
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 4: godot_path 白名单（ADVISORY，GODOT_MCP_ALLOWED_GODOT_PATHS）

**Files:**
- Modify: `src/core/godot-finder.ts`（findGodot/validateGodotBinary/resolveGodotPath 入口）
- Test: `test/core/godot-finder.test.ts`（既有或新）

**Interfaces:**
- Consumes: `GODOT_MCP_ALLOWED_GODOT_PATHS` env（分号分隔，新）、`GODOT_MCP_UNRESTRICTED`（旁路）
- Produces: `isGodotPathAllowed(path): boolean`，findGodot/validateGodotBinary 前置校验

**背景**：godot-finder.ts:55-56 注释自承 `GODOT_MCP_ALLOWED_GODOT_PATHS` 未实现。validateGodotBinary:70 已收紧 --version 签名（伪造成本从"打印 4.6"提到"伪造版本签名"），但 AI 可控的 godot_path 工具参数/project override/env 可指向任意二进制被 spawn（任意代码执行）。路径白名单是签名校验之上的硬隔离层。

- [ ] **Step 1: 写失败测试（RED）**

```typescript
describe('GODOT_MCP_ALLOWED_GODOT_PATHS', () => {
  beforeEach(() => {
    vi.stubEnv('GODOT_MCP_ALLOWED_GODOT_PATHS', '');
    vi.stubEnv('GODOT_MCP_UNRESTRICTED', '');
  });

  it('when set, rejects godot path outside whitelist', () => {
    vi.stubEnv('GODOT_MCP_ALLOWED_GODOT_PATHS', 'C:\\Godot\\bin;D:\\godot');
    expect(isGodotPathAllowed('C:\\malware\\fake-godot.exe')).toBe(false);
  });

  it('when set, allows whitelisted path (realpath normalized)', () => {
    vi.stubEnv('GODOT_MCP_ALLOWED_GODOT_PATHS', join(tmpDir, 'godot'));
    expect(isGodotPathAllowed(join(tmpDir, 'godot', 'godot.exe'))).toBe(true);
  });

  it('when unset (empty), allows any (back-compat,签名校验仍兜底)', () => {
    expect(isGodotPathAllowed('C:\\any\\godot.exe')).toBe(true);
  });

  it('UNRESTRICTED=true bypasses', () => {
    vi.stubEnv('GODOT_MCP_ALLOWED_GODOT_PATHS', 'D:\\only');
    vi.stubEnv('GODOT_MCP_UNRESTRICTED', 'true');
    expect(isGodotPathAllowed('C:\\other')).toBe(true);
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run test/core/godot-finder.test.ts -t "ALLOWED_GODOT_PATHS"`
Expected: FAIL（isGodotPathAllowed 不存在）

- [ ] **Step 3: 实现 isGodotPathAllowed + 接线**

`src/core/godot-finder.ts` 加（复用 path-utils safeRealPath 风格，分号分隔 env）：

```typescript
import { safeRealPath } from './path-utils.js';  // 顶部 import（若未引入）

/**
 * C-SEC-godotpath: GODOT_MCP_ALLOWED_GODOT_PATHS 路径白名单(分号分隔)。
 * 签名校验(isGodotVersionSignature)之上的硬隔离——防 AI 可控的 godot_path
 * 工具参数/project override/env 指向任意二进制被 spawn(任意代码执行)。
 * 空 env = back-compat 放行(签名校验仍兜底);UNRESTRICTED=true 旁路。
 * realpath 归一化(堵 symlink 绕过)。
 */
export function isGodotPathAllowed(candidatePath: string): boolean {
  if (process.env.GODOT_MCP_UNRESTRICTED === 'true') return true;
  const raw = process.env.GODOT_MCP_ALLOWED_GODOT_PATHS;
  if (!raw || raw.trim() === '') return true;  // 未设 = back-compat 放行
  const allowed = raw.split(/[;]+/).map(s => s.trim()).filter(Boolean);
  let realCandidate: string;
  try { realCandidate = safeRealPath(candidatePath); } catch { realCandidate = candidatePath; }
  const isAllowed = allowed.some(a => {
    let realA: string;
    try { realA = safeRealPath(a); } catch { realA = a; }
    return realCandidate === realA || realCandidate.startsWith(realA + sep) || realCandidate.startsWith(realA + '/');
  });
  if (!isAllowed) {
    getLogger().warn('security', `godot path "${candidatePath}" rejected by GODOT_MCP_ALLOWED_GODOT_PATHS whitelist`);
  }
  return isAllowed;
}
```

接线：`validateGodotBinary`（:70）开头 + `detectGodotVersion`（:86）开头 + findGodot 返回最终路径处，加：
```typescript
if (!isGodotPathAllowed(candidatePath)) return false;  // validateGodotBinary
// detectGodotVersion: if (!isGodotPathAllowed(godotPath)) throw new Error(`godot path not in GODOT_MCP_ALLOWED_GODOT_PATHS: ${godotPath}`);
```
核实 findGodot 主流程（findGodot() 返回处）也接入——implementer grep `return godotPath\|return found` 定位所有最终出口，统一加校验。

> 注：`sep` 从 'path' import（确认顶部 import）。

- [ ] **Step 4: 运行验证通过（GREEN）**

Run: `npx vitest run test/core/godot-finder.test.ts`
Expected: 全 PASS

- [ ] **Step 5: 补 defects.ts detect + 文档**

```typescript
{ key: 'godotpath-allowed-paths-unimplemented', status: 'fixed', severity: 'ADVISORY', dimension: 'Security',
  // godot-finder.ts:55 注释自承 GODOT_MCP_ALLOWED_GODOT_PATHS 未实现。签名校验之上的硬隔离缺失。
  // fix: isGodotPathAllowed(env 分号分隔)+ validateGodotBinary/detectGodotVersion/findGodot 接入。
  detect: () => {
    const f = readSrc('src/core/godot-finder.ts');
    return /GODOT_MCP_ALLOWED_GODOT_PATHS/.test(f) && !/isGodotPathAllowed/.test(f) ? 1 : 0;
  } },
```

文档：`docs/telemetry.md` 或 security 相关 doc + README 补 `GODOT_MCP_ALLOWED_GODOT_PATHS` env 说明（local trust 默认空放行，多用户/不可信环境设白名单）。

- [ ] **Step 6: 门禁 + commit**

Run: `npx tsc --noEmit && npx vitest run test/core/godot-finder.test.ts test/regression/defects-fixed.test.ts`
Expected: tsc 0；全绿
Commit:
```bash
git add src/core/godot-finder.ts test/core/godot-finder.test.ts test/regression/defects.ts docs/
git commit -m "feat(security): 实现 GODOT_MCP_ALLOWED_GODOT_PATHS 路径白名单

签名校验之上的硬隔离:isGodotPathAllowed(env 分号分隔,realpath 归一)
接入 validateGodotBinary/detectGodotVersion/findGodot,堵 AI 可控 godot_path
参数指向任意二进制被 spawn。空 env back-compat 放行(签名兜底),UNRESTRICTED 旁路。
+ defects detect + 文档。ADVISORY。
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 5: headless instantiate_class 合并白名单（核心 P1 RCE）+ detect 假绿修正

**Files:**
- Modify: `src/scripts/godot_operations.gd:188-230`（instantiate_class 双分支）
- Modify: `test/regression/defects.ts:608-619`（rce-script-branch-no-node-check detect 假绿修正）+ `:142-147`（set-prop-no-type-whitelist 扩扫 headless）
- Test: GDScript 字面量契约测试（参 recording F2 模式：读 .gd 断言白名单）

**Interfaces:**
- Consumes: ALLOWED_NODE_TYPES（node_commands.gd:6-15）+ ALLOWED_CONTROL_TYPES（ui_commands.gd:9-16）清单
- Produces: `instantiate_class` 纯白名单双分支∈检查，堵 `extends Node` 恶意脚本 _ready RCE

**背景（核心）**：godot_operations.gd:188-230 两分支用 `is_parent_class("Node")` 兜底，但 `Node` 自身是 Node 的父类 → `extends Node` 恶意 class_name 脚本（经 search_and_replace 写盘 + ensureClassNameImport 注册）base_type="Node" 通过 → script.new() → _ready OS.execute RCE，**不经 execute_gdscript 沙箱**。editor 侧 node_commands.gd:280 已是纯白名单（:277-279 注释明说 is_parent_class 兜底=RCE），headless :200-204 自承"未改纯白名单，留产品决策"。

**决策（已确认）**：合并白名单双分支∈检查。`ALLOWED_HEADLESS = ALLOWED_NODE_TYPES ∪ ALLOWED_CONTROL_TYPES ∪ {"Control"} − {"Node"}`。

- [ ] **Step 1: 写 GDScript 字面量契约测试（RED）**

`test/regression/` 下新建或补 `headless-whitelist.test.ts`（读 .gd 源码断言，参 recording F2）：

```typescript
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'fs';
import { join } from 'path';

const GD = readFileSync(join(__dirname, '..', '..', 'src', 'scripts', 'godot_operations.gd'), 'utf-8');

describe('headless instantiate_class whitelist (P1 RCE)', () => {
  it('has ALLOWED_HEADLESS const (merged, no bare Node)', () => {
    expect(GD).toMatch(/const ALLOWED_HEADLESS_TYPES/);
    // 含 Node 系 + Control 系
    expect(GD).toMatch(/"Node2D"/);
    expect(GD).toMatch(/"Control"/);
    expect(GD).toMatch(/"Button"/);  // Control 系代表
  });

  it('ClassDB branch uses whitelist membership (not is_parent_class fallback)', () => {
    const m = GD.match(/if ClassDB\.class_exists[\s\S]{0,400}?(?:return null|return result)/);
    expect(m, 'ClassDB branch found').toBeTruthy();
    expect(m![0]).toMatch(/ALLOWED_HEADLESS_TYPES/);
    // 反向:不再用 is_parent_class 当 ClassDB 分支的充分守卫
    expect(m![0]).not.toMatch(/if not ClassDB\.is_parent_class\(name_of_class,\s*"Node"\)/);
  });

  it('script branch uses base_type whitelist (rejects extends Node)', () => {
    const m = GD.match(/if script is GDScript:[\s\S]{0,600}?return script\.new\(\)/);
    expect(m, 'script branch found').toBeTruthy();
    expect(m![0]).toMatch(/ALLOWED_HEADLESS_TYPES/);
    // 反向:base_type="Node" 被拒(Node 不在白名单)
    expect(m![0]).not.toMatch(/is_parent_class\(\s*base_type\s*,\s*"Node"\s*\)/);
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run test/regression/headless-whitelist.test.ts`
Expected: FAIL（ALLOWED_HEADLESS_TYPES 不存在，分支仍用 is_parent_class）

- [ ] **Step 3: 实现合并白名单 + 双分支∈检查**

`src/scripts/godot_operations.gd` instantiate_class 改（保留 blocked_prefixes 作纵深，但主守卫改白名单）：

```gdscript
# headless instantiate_class 类型白名单(合并 node_commands ALLOWED_NODE_TYPES +
# ui_commands ALLOWED_CONTROL_TYPES + 裸 Control, 移除裸 Node)。
# 对齐 editor node_commands.gd:280 纯白名单精神(I-4)。移除 "Node" 堵 extends Node
# 恶意脚本 _ready RCE(Node 是 Node 的父类, is_parent_class 兜底放行)。
# 须与 addons/.../node_commands.gd + ui_commands.gd 三处白名单同步(defects detect 守护)。
const ALLOWED_HEADLESS_TYPES: Array = [
	"Node3D", "MeshInstance3D", "StaticBody3D", "RigidBody3D",
	"CharacterBody3D", "Camera3D", "Light3D", "DirectionalLight3D",
	"OmniLight3D", "SpotLight3D", "CollisionShape3D", "RayCast3D",
	"Area3D", "Marker3D", "PathFollow3D", "VisibleOnScreenNotifier3D",
	"Node2D", "Sprite2D", "AnimatedSprite2D",
	"CollisionShape2D", "Area2D", "RigidBody2D", "CharacterBody2D",
	"AudioStreamPlayer", "AudioStreamPlayer2D", "AudioStreamPlayer3D",
	"AnimationPlayer", "AnimationTree", "Timer",
	"Control",
	"Button", "Label", "Panel", "LineEdit", "TextEdit", "RichTextLabel",
	"LinkButton", "HSlider", "VSlider", "CheckBox", "CheckButton",
	"OptionButton", "SpinBox", "ProgressBar", "TextureRect", "ColorPickerButton",
	"TabContainer", "Tree", "ItemList", "MarginContainer", "HBoxContainer",
	"VBoxContainer", "GridContainer", "CenterContainer", "ScrollContainer",
	"PanelContainer", "HSplitContainer", "VSplitContainer", "NinePatchRect",
]

func _is_headless_allowed(type_name: String) -> bool:
	return type_name in ALLOWED_HEADLESS_TYPES

func instantiate_class(name_of_class: String):
	if name_of_class.is_empty():
		log_error("Cannot instantiate class: name is empty")
		return null

	# I-S3: 深层纵深——即便白名单,仍阻危险引擎类前缀(白名单已不含,此为防御网)。
	var blocked_prefixes := ["File", "Thread", "Mutex", "Semaphore", "OS", "IP", "StreamPeer", "TCP", "UDP", "HTTP", "TLS", "Crypto", "Hash", "RegEx", "XML", "JSONParser", "ResourceLoader", "ResourceSaver", "PackedData", "TranslationServer", "PhysicsServer", "RenderingServer", "AudioServer", "NavigationServer", "DisplayServer"]
	for prefix in blocked_prefixes:
		if name_of_class.begins_with(prefix):
			log_error(String("Class %s is blocked for security reasons") % name_of_class)
			return null

	if ClassDB.class_exists(name_of_class):
		# P1 RCE 修复(对齐 editor I-4):纯白名单∈检查,不再用 is_parent_class("Node") 兜底。
		# is_parent_class("Node","Node")=true → extends Node 恶意类绕过 → instantiate 跑 _ready RCE。
		# 白名单移除裸 Node, 仅允许具体可实例化类型 + Node2D/Node3D/Control 基础类。
		if not _is_headless_allowed(name_of_class):
			log_error(String("Refused: %s is not in the headless allowed types whitelist") % name_of_class)
			return null
		if not ClassDB.can_instantiate(name_of_class):
			log_error("Class exists but cannot be instantiated: " + name_of_class)
			return null
		var result = ClassDB.instantiate(name_of_class)
		if result == null:
			log_error("ClassDB.instantiate() returned null for class: " + name_of_class)
		return result

	var script = get_script_by_name(name_of_class)
	if script is GDScript:
		# P1 RCE 修复:script 分支同样用白名单——检查脚本 extends 的 base_type ∈ 白名单。
		# extends Node2D 合法自定义脚本 base_type="Node2D" ∈ 允许;
		# extends Node 恶意脚本 base_type="Node" ∉ 允许(白名单移除裸 Node)→ 拒绝, 堵 _ready RCE。
		var base_type: String = script.get_instance_base_type()
		if base_type.is_empty() or not _is_headless_allowed(base_type):
			log_error(String("Refused: script class %s base type %s not in headless whitelist") % [name_of_class, base_type])
			return null
		return script.new()

	log_error("Failed to get script for class: " + name_of_class)
	return null
```

> implementer 用 `edit_script` search_and_replace 模式改（CRLF/tab 安全），改后 `validate_scripts`。

- [ ] **Step 4: 运行验证通过（GREEN）+ check:gdscript 双版本**

Run: `npx vitest run test/regression/headless-whitelist.test.ts`
Expected: 全 PASS

```bash
npm run check:gdscript   # 4.6.3 + 4.7.1 --import 真编译, 0 err 0 warn
```
Expected: 0 err / 0 warn（白名单数组语法 + instantiate 逻辑双版本编译）

- [ ] **Step 5: 修正 detect 假绿（defects.ts:608-619 + :142-147）**

`defects.ts:608-619` rce-script-branch-no-node-check detect 改——不再把 is_parent_class 当充分守卫：

```typescript
{ key: 'rce-script-branch-no-node-check', status: 'fixed', severity: 'CRITICAL', dimension: 'Security',
  // headless instantiate_class 两分支曾用 is_parent_class("Node") 兜底, 但 Node 是 Node 的父类
  // → extends Node 恶意脚本 base_type="Node" 通过 → script.new() _ready RCE(不经 execute_gdscript 沙箱)。
  // fix: 合并白名单双分支∈检查(ALLOWED_HEADLESS_TYPES, 移除裸 Node)。
  // detect: 不再把 is_parent_class 当充分守卫——须验证白名单∈检查存在。
  detect: () => {
    const f = readSrc('src/scripts/godot_operations.gd');
    const hasWhitelist = /ALLOWED_HEADLESS_TYPES/.test(f);
    // script 分支须用 base_type ∈ 白名单(非 is_parent_class 兜底)
    const scriptBranch = f.match(/if script is GDScript:[\s\S]{0,600}?return script\.new\(\)/);
    const scriptUsesWhitelist = scriptBranch && /_is_headless_allowed\(base_type\)/.test(scriptBranch[0]);
    if (!hasWhitelist || !scriptUsesWhitelist) return 1;
    // 反向:确认裸 "Node" 不在白名单(防误加回)
    const wlBlock = f.match(/const ALLOWED_HEADLESS_TYPES[\s\S]{0,900}?\]/);
    if (wlBlock && /^[\t ]*"Node",/m.test(wlBlock[0]) || /,\s*"Node",/.test(wlBlock[0])) {
      // 白名单含裸 Node → extends Node 仍可绕 → 复发
      return 1;
    }
    return 0;
  } },
```

`defects.ts:142-147` set-prop-no-type-whitelist detect 扩扫 headless：

```typescript
{ key: 'set-prop-no-type-whitelist', status: 'fixed', severity: 'CRITICAL', dimension: 'Security',
  detect: () => {
    const addons = countMatchesInDir('addons', /ClassDB\.instantiate\s*\(/g, /\.gd$/);
    const headless = (readSrc('src/scripts/godot_operations.gd').match(/ClassDB\.instantiate\s*\(/g) || []).length;
    const total = addons + headless;
    const whitelist = countMatchesInDir('addons', /_validate_node_type|ALLOWED_BASE_TYPES|ALLOWED_CONTROL_TYPES|is_safe_class/g, /\.gd$/)
      + (/_is_headless_allowed|ALLOWED_HEADLESS_TYPES/.test(readSrc('src/scripts/godot_operations.gd')) ? 1 : 0);
    return total > 0 && whitelist === 0 ? 1 : 0;
  } },
```

- [ ] **Step 6: 运行 defects-fixed 全绿**

Run: `npx vitest run test/regression/defects-fixed.test.ts test/regression/headless-whitelist.test.ts`
Expected: 全绿（detect 修正后仍 = 0）

- [ ] **Step 7: 反向断言（RCE 路径拒绝）+ commit**

补一个端到端反向断言（若 e2e 无法跑恶意脚本，至少 GDScript 字面量测试 Step 1 已含反向"不含 is_parent_class 兜底"）。

Run: `npx tsc --noEmit && npm run check:gdscript`
Expected: tsc 0；check:gdscript 0-0 双版本
Commit:
```bash
git add src/scripts/godot_operations.gd test/regression/headless-whitelist.test.ts test/regression/defects.ts
git commit -m "fix(security): P1 headless instantiate_class 合并白名单堵 extends Node RCE

两分支改 ALLOWED_HEADLESS_TYPES ∈ 检查(= NODE_TYPES ∪ CONTROL_TYPES ∪ Control − Node)。
移除裸 Node 堵 extends Node 恶意脚本 base_type='Node' 绕 is_parent_class 兜底 →
script.new() _ready RCE(不经 execute_gdscript 沙箱)。对齐 editor node_commands I-4。
+ detect 假绿修正(不再把 is_parent_class 当充分守卫) + set-prop detect 扩扫 headless。
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 6: U1 proxy 披露文档

**Files:**
- Modify: `docs/telemetry.md`（update-checker 段）、`README.md:73`、`README.en.md:69`

**Interfaces:** 无代码改动。纯文档诚实披露。

**背景**：update-checker.ts:86 fetch 默认读 HTTP_PROXY/HTTPS_PROXY/NO_PROXY（Node 默认 trustEnv），四份披露文档未提代理 env → 披露完整性缺口。**修复=文档补充，勿改 trustEnv=false**（会断企业代理用户访问 npm）。NO_PROXY 为 workaround。

- [ ] **Step 1: 核实 update-checker.ts:86 现状**

Run: 读 `src/core/update-checker.ts:80-95`，确认 fetch 调用未设 `trustEnv: false`（即默认遵守代理 env）。

- [ ] **Step 2: 补文档**

`docs/telemetry.md`（update-checker 段）补一句：
```markdown
> **代理环境**：update-checker 的 npm registry fetch 遵守 `HTTP_PROXY`/`HTTPS_PROXY`/`NO_PROXY` 环境变量（Node 默认 `trustEnv`）。这意味着：(1) 企业代理环境下请求经代理；(2) 完全阻断可设 `NO_PROXY=registry.npmjs.org` 或防火墙规则。**刻意不设 `trustEnv: false`**——那会切断合法企业代理用户的更新检查。
```

`README.md:73`（telemetry/update-checker 段）+ `README.en.md:69` 对应段补对应对称说明（中英）。

- [ ] **Step 3: 核实无遗漏披露点**

grep update-checker 相关披露（`docs/telemetry.md` + README 中英 + CLAUDE.md rule），确认四处一致提及 proxy env。

- [ ] **Step 4: commit（无 detect，纯文档）**

Run: `npx tsc --noEmit`（确认未误改代码）
Commit:
```bash
git add docs/telemetry.md README.md README.en.md
git commit -m "docs(telemetry): U1 update-checker 披露 proxy env 遵守

补 HTTP_PROXY/HTTPS_PROXY/NO_PROXY 说明(trustEnv 默认),企业代理环境请求经代理,
完全阻断设 NO_PROXY=registry.npmjs.org。刻意不改 trustEnv=false(断企业代理用户)。
Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 7: 全量门禁收尾 + CHANGELOG + build-matrix

**Files:**
- Modify: `test/regression/defects.ts`（头注 FIXED 计数实测更新）、`CHANGELOG.md`（[Unreleased] A-RCE 段）、`docs/capability-matrix.md`（若 GUARDED_KEYS/risk 变，本批无新工具故可能无变更）

**目的**：确认 6 条 finding 全闭环，门禁全绿，CHANGELOG 记录，defects 计数实测对齐。

- [ ] **Step 1: 全量门禁**

```bash
cd D:/GitHub/godot-mcp-enhanced
npx tsc --noEmit
npx eslint src --max-warnings 999
npm run check:gdscript          # 4.6.3 + 4.7.1 --import 真编译
npm test                         # 全量 vitest
npm run test:regression          # defects-fixed + defects-open
```
Expected: tsc 0 / eslint 0 errors（warnings 须确认非本次引入）/ check:gdscript 0-0 / vitest 全 passed（4 pre-existing T11 elicitation 确认非回归）/ defects-fixed 全绿

- [ ] **Step 2: defects.ts 头注计数实测**

grep `defects.ts` 头注 FIXED 计数，与新增 detect 条数对齐（A-telemetry 后 101，本批 +N detect：S1/bpy/profile/godot_path/headless-detect修正 = 约 +4~5 新 FIXED 条目，实测为准）。更新头注。

- [ ] **Step 3: CHANGELOG [Unreleased] A-RCE 段**

```markdown
## [Unreleased]
### Security (A-RCE batch)
- **P1 headless instantiate_class 合并白名单**：移除 `is_parent_class("Node")` 兜底，改 `ALLOWED_HEADLESS_TYPES`（NODE_TYPES ∪ CONTROL_TYPES ∪ Control − Node）双分支∈检查，堵 `extends Node` 恶意脚本 _ready RCE（不经 execute_gdscript 沙箱）。BREAKING：`create_scene(root_node_type="Node")` / `add_node(node_type="Node")` 被拒（罕见，用 Node2D/Node3D 或 execute_gdscript）。
- **P1 self_update dest 符号链接校验**：updateAddon/readAddonVersion 补 `safeRealPath(dest)` + `isPathInAllowedRoots`，堵 `addons/` 子段符号链接 cpSync 跟随写出 allowlist 外。
- **P2 execute_bpy 危险 API 扫描**：新增 `scanBpySandbox`（os/subprocess/eval/exec/__import__/ctypes），对齐 execute_gdscript 纵深防御。
- **P2 profile 硬隔离**：`executeToolCall` 入口加 `isToolAllowed` 强制检查。
- **ADVISORY godot_path 白名单**：实现 `GODOT_MCP_ALLOWED_GODOT_PATHS` env（分号分隔，realpath 归一），接入 validateGodotBinary/detectGodotVersion/findGodot。
- **detect 假绿修正**：`rce-script-branch-no-node-check` 不再把 is_parent_class 当充分守卫；`set-prop-no-type-whitelist` 扩扫 headless。
- **U1 文档**：update-checker 披露 HTTP_PROXY/HTTPS_PROXY/NO_PROXY 遵守。
```

- [ ] **Step 4: build-matrix（若需要）**

本批无新工具/组，GUARDED_KEYS 未变（execute_bpy 已是 process）。运行确认无 drift：
```bash
npm run build-matrix && git diff --stat docs/capability-matrix.md
```
Expected: 无 drift（若有则重生 + 确认合理）

- [ ] **Step 5: final commit + requesting-code-review**

```bash
git add test/regression/defects.ts CHANGELOG.md docs/capability-matrix.md
git commit -m "docs(a-rce): 全量门禁收尾 + defects 计数 + CHANGELOG

A-RCE 6 finding 全闭环。tsc0/eslint0/check:gdscript 0-0/vitest 全 passed/
defects-fixed 全绿。CHANGELOG [Unreleased] Security 段 + detect 计数实测对齐。
Co-Authored-By: Claude <noreply@anthropic.com>"
```

然后 `superpowers:requesting-code-review`（final review 用 opus），跨 task 核实：白名单三处一致性 / safeRealPath 首装安全 / isToolAllowed 不误拒 core / bpy 扫描对齐度 / detect 反假绿有效。

---

## Self-Review

**1. Spec coverage（对总 spec A 批次 RCE 6 条 + writing-plans 待核实点 :75-83）：**
- ✅ S1 self_update 符号链接 → T1（待核实点 safeRealPath 首装行为 + resolveWithinRoot 已核实）
- ✅ headless instantiate_class 白名单 + detect 假绿 → T5（待核实点 node_commands/ui_commands 白名单清单 + godot_operations:202-203 Control 回归产品决策已解决）
- ✅ profile 硬隔离 → T3（待核实点 executeToolCall:226 + isToolAllowed:183 已核实）
- ✅ bpy 扫描 → T2（待核实点 blender.ts:93 + scanGdscriptSandbox 模式已核实）
- ✅ godot_path 白名单 → T4（待核实点 godot-finder.ts:55-56 + validateGodotBinary 已核实）
- ✅ U1 proxy 披露 → T6
- ✅ 每条补 detect（spec 通用约束 :60）→ T1/T2/T3/T4/T5 各含 detect step

**2. Placeholder scan：** 无 TBD/TODO；每步含实际代码。detect 正则 + 白名单清单 + 文档措辞均具体。

**3. Type consistency：**
- `scanBpySandbox(code): string[]`（T2 定义）↔ blender.ts 接线（T2 Step4）一致
- `isGodotPathAllowed(path): boolean`（T4 定义）↔ validateGodotBinary/detectGodotVersion 接线一致
- `isToolAllowed(name)`（tool-registry.ts:262 既有）↔ ToolDispatcher executeToolCall（T3）一致
- `_is_headless_allowed(type_name)`（T5 GD 定义）↔ ClassDB/script 双分支调用一致
- `safeRealPath(dest)` / `isPathInAllowedRoots(realDest)`（path-utils.ts 既有）↔ addon-version（T1）一致

**关键风险（review 重点）：**
- T5 白名单移除裸 Node 是 BREAKING（create_scene/add_node 用 "Node" 失败）——已在决策 + CHANGELOG 标注
- T3 isToolAllowed 入口强制可能误拒——TDD Step1 先核实 core 工具基线
- T1 cpSync 目标用 dest 还是 realDest——implementer 简化（始终 dest，realDest 仅校验）
- 三处白名单（node_commands/ui_commands/godot_operations）DRY 漂移——detect 守护 + 注释强调
