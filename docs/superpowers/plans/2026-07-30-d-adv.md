# D-ADV（报告5 ADVISORY）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 2026-07-29 addons-GDScript 审查（报告5）的 ADVISORY 级缺陷 F5/F6/F9（F7 核实已由 C11 闭环、F8 核实 won't-fix，见下）。

**Architecture:** 3 个相互独立的机械 GDScript 修复（animtree conditions 类型守卫 / 删 undo_manager 死代码 / asset_placer undo_mgr null 守卫），每条 TDD 加 defects.ts detect 防复发 + 计数三处同步。不改工具签名/路由，不动 editor 路由接线。

**Tech Stack:** GDScript（Godot 4.x `addons/godot_mcp_server/`）、Vitest（`test/regression/defects-fixed.test.ts` 静态 detect）。

## 核实结论（writing-plans 建基线，[[plan-baseline-verify-grep]][[verify-implementation-by-source]]）

报告5 ADVISORY 5 条逐项源码核实：
- **F5（真）** `animtree_commands.gd:136-138` `for cond in conditions:` 后 `cond.get("name","")` 假设 cond 是 Dictionary；非 Dictionary 元素（String/int/Array）→ SCRIPT ERROR 中断 transition 建立。
- **F6（真）** `undo_manager.gd:9 create_action` + `:20 create_action_with_props` 零外部调用（全仓 grep 仅 `create_action_mixed` 在用；test/ 无引用——`theme_create_action` 是无关 UI 参数）；`_add_method_call`（:58）被 `_apply_op`（:69）调用故保留。
- **F7（stale，no-op）** `node_commands.gd:267-271` 已有「扫 validated，未入树 cls free」孤儿清理，覆盖 `_undo_manager != null` 与 else 两分支（C11 闭环）——报告 finding 滞后。本批不动。
- **F8（won't-fix）** `plugin.gd:7,18` `_enter_tree/_exit_tree` 不调 `super()` 是**正确的**：CLAUDE.md engine rule「原生类虚函数禁 super()——EditorPlugin/Node 虚函数（_ready/_process/_enter_tree/_exit_tree 含 _init）super() 一律 Parse Error」（2026-07-04 移除 654b162 回归先例）。加 super() 反而破坏编译。本批不动。
- **F9（真）** `asset_placer.gd:45,85` `undo_mgr.create_action_mixed(...)` 无 null 守卫；headless（undo_mgr==null）→ null instance SCRIPT ERROR。对齐 `node_commands.gd:260-265` 的 `if _undo_manager != null: ... else: callv fallback`。

## Global Constraints（继承总 spec 通用约束 + 项目规则）

- 工作仓库 = `D:\GitHub\godot-mcp-enhanced`（非 kit 子模块）；**master 本地 commit，不 push origin**。
- 精确编辑（Karpathy）：只动被要求部分，匹配 GDScript 风格（tab 缩进、`#` 注释）。
- **每修一条补 `defects.ts` detect 防复发** + 计数三处同步（`defects.ts:2` 头注 + `defects-fixed.test.ts:142` + `:144` 两处 `toBe`）。基线写前 grep 实测（[[plan-baseline-verify-grep]]）：当前 FIXED_DEFECTS = **123**（D-P2 后；defects.ts:2 + toBe(123)×2 一致；defects-fixed 124 passed = 123 detect + 1 结构）。
- TDD：先写 detect（RED，fail）→ 再改 .gd（GREEN，pass），须有 RED→GREEN 证据。
- `.gd` 改后跑 `npm run check:gdscript`（全量 0 err / 0 warn）。
- 跨文件改动查调用链（F6 删死代码前确认零外部调用——已核实）。
- editor 运行时实测（undo Ctrl+Z / headless null 路径）属 editor-only 环境，留待人工；静态语义 + check:gdscript + detect GREEN 为可交付证据。

---

### Task 1: F5 animtree add_transition conditions Dictionary 守卫

**Files:**
- Modify: `D:\GitHub\godot-mcp-enhanced\addons\godot_mcp_server\commands\animtree_commands.gd:135-140`
- Modify: `D:\GitHub\godot-mcp-enhanced\test\regression\defects.ts`（加 detect + 头注计数 123→124）
- Modify: `D:\GitHub\godot-mcp-enhanced\test\regression\defects-fixed.test.ts:142,144`（toBe 123→124）

**Interfaces:** 无跨 task 依赖。

- [ ] **Step 1: 写 detect 闭包（RED）**

在 `defects.ts` `FIXED_DEFECTS` 数组末尾（`websocket-outbound-no-buffer-limit` 条目后、`]` 前）加：

```typescript
  { key: 'animtree-conditions-no-dict-guard', status: 'fixed', severity: 'ADVISORY', dimension: 'Correctness',
    // P2(2026-07-29 报告5 F5): animtree add_transition conditions 元素未校验 Dictionary，
    // 非 Dictionary 输入 cond.get() → SCRIPT ERROR 中断 transition 建立。fix: for 循环内加 cond is Dictionary 守卫跳过非法元素。
    // detect: animtree_commands.gd 无 'cond is Dictionary' = 复发。
    detect: () => {
      return /cond is Dictionary/.test(readSrc('addons/godot_mcp_server/commands/animtree_commands.gd')) ? 0 : 1;
    } },
```

计数同步：`defects.ts:2` 头注 `FIXED_DEFECTS 123 条` → `124 条` + 末尾追加 F5 描述；`defects-fixed.test.ts:142` `toBe(123)` → `toBe(124)`；`:144` `toBe(123)` → `toBe(124)`。

Run: `cd /d/GitHub/godot-mcp-enhanced && npx vitest run test/regression/defects-fixed.test.ts -t animtree-conditions`
Expected: FAIL（detect 命中 1——`cond is Dictionary` 当前 0 处）

- [ ] **Step 2: 加 Dictionary 守卫**

`animtree_commands.gd:135-137` 当前：

```gdscript
	if conditions != null and conditions is Array:
		for cond in conditions:
			var cond_name: String = str(cond.get("name", ""))
```

改为（for 循环体内、`cond.get` 前插入守卫）：

```gdscript
	if conditions != null and conditions is Array:
		for cond in conditions:
			# F5(2026-07-29 报告5): conditions 元素须为 Dictionary，否则 cond.get() SCRIPT ERROR 中断 transition。
			if not (cond is Dictionary):
				continue
			var cond_name: String = str(cond.get("name", ""))
```

（`:138 var cond_value = cond.get("value")` 及后续不变。）

- [ ] **Step 3: detect 通过 + check:gdscript**

Run: `npx vitest run test/regression/defects-fixed.test.ts -t animtree-conditions`
Expected: PASS（detect===0）

Run: `npm run check:gdscript`
Expected: errors=0 warnings=0

- [ ] **Step 4: commit**

```bash
git add addons/godot_mcp_server/commands/animtree_commands.gd test/regression/defects.ts test/regression/defects-fixed.test.ts
git commit -m "fix(correctness): F5 animtree conditions Dictionary 守卫（D-ADV Task1）"
```

---

### Task 2: F6 删 undo_manager 死代码 create_action + create_action_with_props

**Files:**
- Modify: `D:\GitHub\godot-mcp-enhanced\addons\godot_mcp_server\undo_manager.gd:9-28`（删两方法）
- Modify: `D:\GitHub\godot-mcp-enhanced\test\regression\defects.ts`（加 detect + 头注计数 124→125）
- Modify: `D:\GitHub\godot-mcp-enhanced\test\regression\defects-fixed.test.ts:142,144`（toBe 124→125）

**Interfaces:** 无。`create_action_mixed`（:35）是唯一在用的 undo 入口，不动；`_add_method_call`（:58）被 `_apply_op`（:69）调用，保留。

**核实前置（已完成）**：全仓 grep `\.create_action\(|create_action_with_props` 仅命中 `undo_redo.create_action(`（Godot 内部 API）+ 两方法定义自身；test/ 无引用（`theme_create_action` 是无关 UI 参数）。零外部调用，删除安全。

- [ ] **Step 1: 写 detect 闭包（RED）**

`FIXED_DEFECTS` 末尾加：

```typescript
  { key: 'undo-manager-dead-create-action', status: 'fixed', severity: 'ADVISORY', dimension: 'Maintainability',
    // P2(2026-07-29 报告5 F6): undo_manager.create_action + create_action_with_props 零外部调用（仅 create_action_mixed 在用），
    // 死代码维护负担 + 误导调用者用旧 API。fix: 删两方法（_add_method_call 保留，被 _apply_op 用）。
    // detect: 两方法定义仍在 = 复发（注意 func create_action( 不匹配 func create_action_mixed(）。
    detect: () => {
      const src = readSrc('addons/godot_mcp_server/undo_manager.gd');
      return (/func create_action\(/.test(src) || /func create_action_with_props/.test(src)) ? 1 : 0;
    } },
```

计数同步三处：头注 `124 条` → `125 条`；`:142` / `:144` `toBe(124)` → `toBe(125)`。

Run: `npx vitest run test/regression/defects-fixed.test.ts -t undo-manager-dead`
Expected: FAIL（两方法当前存在，detect 命中 1）

- [ ] **Step 2: 删两死方法**

`undo_manager.gd:8-30` 当前（`setup` 后到 `create_action_mixed` 注释前）：

```gdscript

func create_action(request_id: int, do_methods: Array, undo_methods: Array) -> void:
	var undo_redo = _plugin.get_undo_redo()
	undo_redo.create_action("MCP: op_%d" % request_id)
	for m in do_methods:
		_add_method_call(undo_redo, "do", m)
	for m in undo_methods:
		_add_method_call(undo_redo, "undo", m)
	undo_redo.commit_action()


## 创建带 property 操作的 undo action
func create_action_with_props(request_id: int, do_props: Array, undo_props: Array) -> void:
	var undo_redo = _plugin.get_undo_redo()
	undo_redo.create_action("MCP: op_%d" % request_id)
	for p in do_props:
		undo_redo.add_do_property(p.target, p.property, p.value)
	for p in undo_props:
		undo_redo.add_undo_property(p.target, p.property, p.value)
	undo_redo.commit_action()


## 创建混合 action（methods + properties + references）
```

删成（保留 `setup` 后一空行 + 直接接 `## 创建混合 action` 注释）：

```gdscript


## 创建混合 action（methods + properties + references）
```

（即删除 `func create_action(...)` 整方法 + `## 创建带 property 操作的 undo action` 注释 + `func create_action_with_props(...)` 整方法。`create_action_mixed`（原 :35）及其后的 `_add_method` / `_add_method_call` / `_apply_op` 全部保留。）

- [ ] **Step 3: detect 通过 + check:gdscript + 调用链复查**

Run: `npx vitest run test/regression/defects-fixed.test.ts -t undo-manager-dead`
Expected: PASS（detect===0，两方法已删）

调用链复查：`grep -rn "create_action_with_props\|\.create_action(" addons/ src/ test/` 仅应剩 `undo_redo.create_action(`（undo_manager 内部）+ `theme_create_action`（无关）。

Run: `npm run check:gdscript`
Expected: 0/0

- [ ] **Step 4: commit**

```bash
git add addons/godot_mcp_server/undo_manager.gd test/regression/defects.ts test/regression/defects-fixed.test.ts
git commit -m "refactor(undo): F6 删 undo_manager 死代码 create_action+with_props（D-ADV Task2）"
```

---

### Task 3: F9 asset_placer place_one/place_batch undo_mgr null 守卫

**Files:**
- Modify: `D:\GitHub\godot-mcp-enhanced\addons\godot_mcp_server\commands\asset\asset_placer.gd:45`（place_one）+ `:85`（place_batch）
- Modify: `D:\GitHub\godot-mcp-enhanced\test\regression\defects.ts`（加 detect + 头注计数 125→126）
- Modify: `D:\GitHub\godot-mcp-enhanced\test\regression\defects-fixed.test.ts:142,144`（toBe 125→126）

**Interfaces:** 无。对齐 `node_commands.gd:260-265` 的 `if _undo_manager != null: create_action_mixed / else: callv fallback` 模式。

- [ ] **Step 1: 写 detect 闭包（RED）**

`FIXED_DEFECTS` 末尾加：

```typescript
  { key: 'asset-placer-no-undo-mgr-null-guard', status: 'fixed', severity: 'ADVISORY', dimension: 'Reliability',
    // P2(2026-07-29 报告5 F9): asset_placer place_one/place_batch 直接 undo_mgr.create_action_mixed 无 null 守卫，
    // headless（undo_mgr==null）null instance SCRIPT ERROR。fix: 对齐 node_commands:260 if undo_mgr != null + else callv fallback。
    // detect: asset_placer.gd 'undo_mgr != null' 出现 < 2 = 复发（place_one + place_batch 各一）。
    detect: () => {
      const n = (readSrc('addons/godot_mcp_server/commands/asset/asset_placer.gd').match(/undo_mgr != null/g) || []).length;
      return n >= 2 ? 0 : 1;
    } },
```

计数同步三处：头注 `125 条` → `126 条`；`:142` / `:144` `toBe(125)` → `toBe(126)`。

Run: `npx vitest run test/regression/defects-fixed.test.ts -t asset-placer-no-undo`
Expected: FAIL（当前 0 处 `undo_mgr != null`，detect 命中 1）

- [ ] **Step 2: place_one 加守卫 + fallback**

`asset_placer.gd:45-46` 当前：

```gdscript
	undo_mgr.create_action_mixed("asset_create_%d" % request_id, do_ops, undo_ops)
	return {"result": {"node_path": str(node.get_path())}}
```

改为：

```gdscript
	if undo_mgr != null:
		undo_mgr.create_action_mixed("asset_create_%d" % request_id, do_ops, undo_ops)
	else:
		# F9(2026-07-29 报告5): headless 无 undo_mgr，直接执行 do_ops 落地节点（对齐 node_commands:262-265）。
		for op in do_ops:
			if String(op.get("type", "method")) == "method":
				op["target"].callv(op["method"], op["args"])
	return {"result": {"node_path": str(node.get_path())}}
```

- [ ] **Step 3: place_batch 加守卫 + fallback**

`asset_placer.gd:85` 当前：

```gdscript
	undo_mgr.create_action_mixed("asset_batch_%d" % request_id, do_ops, undo_ops)
```

改为：

```gdscript
	if undo_mgr != null:
		undo_mgr.create_action_mixed("asset_batch_%d" % request_id, do_ops, undo_ops)
	else:
		# F9(2026-07-29 报告5): headless 无 undo_mgr，直接执行 do_ops 落地全部节点。
		for op in do_ops:
			if String(op.get("type", "method")) == "method":
				op["target"].callv(op["method"], op["args"])
```

（`:86` 后 `# create_action_mixed 已 commit...` 注释 + `for node in nodes: node_paths.append(...)` 不变——两分支执行后节点均已 add_child 入树，get_path 有效。）

- [ ] **Step 4: detect 通过 + check:gdscript**

Run: `npx vitest run test/regression/defects-fixed.test.ts -t asset-placer-no-undo`
Expected: PASS（detect===0，2 处 `undo_mgr != null`）

Run: `npm run check:gdscript`
Expected: 0/0

- [ ] **Step 5: commit**

```bash
git add addons/godot_mcp_server/commands/asset/asset_placer.gd test/regression/defects.ts test/regression/defects-fixed.test.ts
git commit -m "fix(reliability): F9 asset_placer undo_mgr null 守卫（D-ADV Task3）"
```

---

## 验收（Task 3 后）

- 全量门禁：`npm run build`（tsc 0）+ `npm run lint`（0 err）+ `npm run check:gdscript`（0/0）+ `npx vitest run`（全绿，defects-fixed 应 **127 passed = 126 detect + 1 结构**）
- final review opus（整支 `<D-ADV baseline>..HEAD`）
- 项目待办.md 报告5 ADVISORY 段回标 F5/F6/F9（[x] + commit）、F7 标注「C11 已闭环」、F8 标注「won't-fix（super 禁）」
- Obsidian 开发日志 + memory hook
- master 本地，不 push

## Self-Review

- **Spec 覆盖**：报告5 ADVISORY 5 条——F5(Task1)/F6(Task2)/F9(Task3) 有 task；F7(stale,no-op)/F8(won't-fix) 在「核实结论」段文档化。无遗漏。
- **占位符扫描**：每步含实际代码 / 命令 / 期望输出，无 TBD。
- **类型/签名一致**：`undo_mgr`（asset_placer 静态参数名，与现有一致）/ `_undo_manager`（node_commands 实例字段）——本批不混用；detect regex `func create_action\(` 不误匹 `create_action_mixed(`（`(` vs `_`）；计数链 123→124→125→126 三处同步。
