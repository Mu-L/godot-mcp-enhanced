# v0.20.0 全工具验证靶子 + 自动化测试 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 升级 0.19.1 → 0.20.0；建无 autoload 的真实多子系统靶子 `test\fixtures\real-project\`；扩建 `e2e-full-tool-verification.test.ts` 三层全自动化验证 28 个顶层工具正路径。

**Architecture:** 靶子用 MCP scene 工具创建（兼验证工具本身）；测试复用现有 `callTool` helper + tool-registry 直调 `handleTool`；L1 切 real-project 升级正路径断言，L2 真启 Godot 进程跑 bridge/recording/profiler/editor，L3 配环境跑 multi_instance/dynamic/android；反假绿沿用 IMPORTANT-9b stderr 模式。

**Tech Stack:** vitest 4.1 / TypeScript / Godot 4.6.2 / MCP tool-registry / Bridge TCP 9081 / Editor WebSocket 9090

## Global Constraints

- `GODOT_PATH=D:\godot\Godot_v4.6.2-stable_win64.exe`（已配）
- `ALLOWED_PROJECT_PATHS` 含 `D:/GitHub`（仓库内靶子免配白名单）
- 版本真相源 = `package.json`；`npm run version-sync` 同步 A 类 3 文件（manifest/plugin.cfg/使用指南），B 类 2 文件（CHANGELOG/README）手写，`npm run version-check` 校验 5 文件一致
- 所有 Godot 依赖测试：`describe.skipIf(!hasGodot || !hasProject)` + `process.stderr.write` 告警（vitest 吞 console.*）
- editor 合取守卫：`skipIf(!hasGodot || !process.env.E2E_EDITOR)`
- 文档/注释引用文件用绝对路径
- 不 push origin（本地领先，用户惯例；push 须显式确认）
- 现有 `callTool(toolName, args)` 签名：`{ project_path: TEST_PROJECT, ...args }` → `handleTool` → `{ text, isError }`（见 `test\e2e-full-tool-verification.test.ts:76-84`）

## File Structure

| 文件 | 责任 | 操作 |
|------|------|------|
| `test\fixtures\real-project\project.godot` | 无 autoload 项目配置 | Create |
| `test\fixtures\real-project\scenes\2d\main_2d.tscn` | 2D 多子系统场景 | Create |
| `test\fixtures\real-project\scenes\3d\main_3d.tscn` | 3D 多子系统场景 | Create |
| `test\fixtures\real-project\scenes\audio_demo.tscn` | audio/signal 场景 | Create |
| `test\fixtures\real-project\scripts\*.gd` | 脚本 + 信号定义 | Create |
| `test\fixtures\real-project\materials\default.tres` | StandardMaterial3D | Create |
| `test\e2e-full-tool-verification.test.ts` | 三层验证主文件 | Modify（扩建） |
| `package.json` / `manifest.json` / `addons\godot_mcp_server\plugin.cfg` / `docs\使用指南.md` / `CHANGELOG.md` / `README.md` | 版本元数据 | Modify（Task 1） |

---

### Task 1: 版本升级 0.19.1 → 0.20.0

**Files:**
- Modify: `D:\GitHub\godot-mcp-enhanced\package.json`（version 字段）
- Modify（自动同步）: `manifest.json` / `addons\godot_mcp_server\plugin.cfg` / `docs\使用指南.md`
- Modify（手动）: `CHANGELOG.md` / `README.md`

**Interfaces:** Produces: 0.20.0 版本基线，后续 Task 在此之上。

- [ ] **Step 1: 改真相源**

`package.json:3`：`"version": "0.19.1"` → `"version": "0.20.0"`

- [ ] **Step 2: 同步 A 类 3 文件**

Run: `npm run version-sync`
Expected: 输出 3 行 `manifest.json / addons\.../plugin.cfg / docs\使用指南.md: 0.19.1 → 0.20.0`，末尾 `✓ A 类元数据已同步到 0.20.0`

- [ ] **Step 3: 手动 CHANGELOG**

`CHANGELOG.md` 第 7 行 `[Unreleased]` 下插入：

```markdown
## [0.20.0] - 2026-06-30

### Added — cpp GDExtension 脚手架 + 全工具验证靶子

- **cpp `scaffold_gdextension`**：新工具，生成 8 文件 C++ GDExtension 工程骨架（不联网/不编译），填补竞品 C++/GDExtension 赛道空白
- **全工具验证靶子**：`test\fixtures\real-project\` 真实多子系统无 autoload 靶子 + 三层（L1 headless 静态 / L2 运行态 / L3 特殊环境）全自动化验证，覆盖 28/30 顶层工具正路径
- 含 R3 审查修复、全面安全审查 H1-M4 修复（累计自 v0.19.1 的本地增量）
```

- [ ] **Step 4: 手动 README 版本表**

`README.md` 版本表顶部插入 `| **v0.20.0** | ... |` 行（格式对齐现有 `| **v0.19.1** |` 行）。

- [ ] **Step 5: 校验 5 文件一致**

Run: `npm run version-check`
Expected: `✓ 版本元数据一致 (0.20.0)`，exit 0

- [ ] **Step 6: 确认 matrix 无漂移**

Run: `npm run build-matrix`
Expected: `docs\capability-matrix.md:6` 仍 `工具总数：30`，`cpp (code)` 在列

- [ ] **Step 7: 跑 version-sync 测试**

Run: `npx vitest run test/version-sync.test.ts`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add package.json manifest.json addons/godot_mcp_server/plugin.cfg "docs/使用指南.md" CHANGELOG.md README.md docs/capability-matrix.json docs/capability-matrix.md test/version-sync.test.ts
git commit -m "chore(release): v0.20.0 cpp 脚手架 + 全工具验证靶子

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 靶子骨架 + 2D 场景

**Files:**
- Create: `test\fixtures\real-project\project.godot`
- Create: `test\fixtures\real-project\scenes\2d\main_2d.tscn`
- Create: `test\fixtures\real-project\scripts\main_2d.gd`
- Create: `test\fixtures\real-project\materials\default.tres`

**Interfaces:**
- Produces: `main_2d.tscn` 含 `Camera2D` / `TileMapLayer` / `VBoxContainer`(Button+Label) / `GPUParticles2D` / `AnimationPlayer`，供 ui/tilemap/animation/particles/screenshot L1 正路径用

- [ ] **Step 1: 建干净 project.godot（无 autoload）**

用 `scene` 工具或直接写文件，内容：

```ini
config_version=5
[application]
config/name="real-project"
run/main_scene="res://scenes/2d/main_2d.tscn"
[ Rendering ]
renderer/rendering_method="gl_compatibility"
```

- [ ] **Step 2: 用 MCP quick_scene 创建 main_2d 骨架**

```
scene(action=quick_scene, scene_path="res://scenes/2d/main_2d.tscn",
  root_node_type="Node2D", root_node_name="Main2D")
```

- [ ] **Step 3: add_node 加 2D 子系统节点**

依次调用 `scene(action=add_node, scene_path="res://scenes/2d/main_2d.tscn", parent_node_path=".")`：

| node_type | node_name | properties |
|-----------|-----------|-----------|
| Camera2D | Camera2D | `{}` |
| TileMapLayer | TileLayer | `{}` |
| GPUParticles2D | Particles2D | `{ amount: 8 }` |
| AnimationPlayer | AnimPlayer | `{}` |
| VBoxContainer | UIBox | `{ layout: ... }`（用 ui build_layout 更佳） |

- [ ] **Step 4: 用 ui build_layout 建 UI 子树**

```
ui(action=build_layout, scene_path="res://scenes/2d/main_2d.tscn", parent_path=".",
  tree={ type:"VBoxContainer", name:"UIBox",
    layout:{ direction:"column", gap:8 },
    children:[
      { type:"Label", name:"Title", properties:{ text:"Real Project 2D" } },
      { type:"Button", name:"ActionBtn", properties:{ text:"Click" } }
    ]})
```

- [ ] **Step 5: 写 main_2d.gd（带信号定义）**

`scripts/main_2d.gd`：

```gdscript
extends Node2D

signal action_pressed(value: int)

func _ready() -> void:
    pass

func do_action(v: int) -> void:
    action_pressed.emit(v)
```

- [ ] **Step 6: save_scene 持久化**

```
scene(action=save_scene, scene_path="res://scenes/2d/main_2d.tscn")
```

- [ ] **Step 7: 建 materials/default.tres**

```ini
[gd_resource type="StandardMaterial3D" format=3]
[resource]
albedo_color = Color(0.8, 0.2, 0.2, 1)
```

- [ ] **Step 8: 校验场景可加载**

Run（headless）：`scene(action=query_scene_tree, scene_path="res://scenes/2d/main_2d.tscn")`
Expected: 返回含 Camera2D/TileLayer/UIBox/Particles2D/AnimPlayer 的树

- [ ] **Step 9: Commit**

```bash
git add test/fixtures/real-project/
git commit -m "feat(fixture): real-project 2D 多子系统靶子(无 autoload)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 靶子 3D + audio 场景

**Files:**
- Create: `test\fixtures\real-project\scenes\3d\main_3d.tscn`
- Create: `test\fixtures\real-project\scenes\audio_demo.tscn`
- Create: `test\fixtures\real-project\scripts\audio_host.gd`

**Interfaces:**
- Produces: `main_3d.tscn` 含 `MeshInstance3D` / `DirectionalLight3D` / `Camera3D` / `NavigationRegion3D` / `RigidBody3D` / `GPUParticles3D`，供 nav/physics/material/screenshot 3D 正路径；`audio_demo.tscn` 含 `AudioStreamPlayer`，供 audio/signal

- [ ] **Step 1: quick_scene 3D 骨架**

```
scene(action=quick_scene, scene_path="res://scenes/3d/main_3d.tscn",
  root_node_type="Node3D", root_node_name="Main3D")
```

- [ ] **Step 2: create_3d_node 加 3D 节点**

调用 `scene(action=create_3d_node, scene_path="res://scenes/3d/main_3d.tscn", parent=".")`：

| type | name |
|------|------|
| Camera3D | Camera3D |
| DirectionalLight3D | Light3D |
| MeshInstance3D | TestMesh |
| RigidBody3D | Body3D |
| GPUParticles3D | Particles3D |
| NavigationRegion3D | NavRegion |

- [ ] **Step 3: save_scene 3D**

```
scene(action=save_scene, scene_path="res://scenes/3d/main_3d.tscn")
```

- [ ] **Step 4: quick_scene audio_demo**

```
scene(action=quick_scene, scene_path="res://scenes/audio_demo.tscn",
  root_node_type="Node", root_node_name="AudioDemo")
```

- [ ] **Step 5: 加 AudioStreamPlayer + 信号宿主**

```
scene(action=add_node, scene_path="res://scenes/audio_demo.tscn", parent_node_path=".",
  node_type="AudioStreamPlayer", node_name="BgmPlayer", properties={})
scene(action=add_node, scene_path="res://scenes/audio_demo.tscn", parent_node_path=".",
  node_type="Node", node_name="SignalHost", properties={})
```

`scripts/audio_host.gd`：

```gdscript
extends Node

signal beep(freq: float)
```

把 SignalHost 的 script 设为 `audio_host.gd`（edit_node properties: `{ script: "res://scripts/audio_host.gd" }`）。

- [ ] **Step 6: save_scene audio + 校验**

```
scene(action=save_scene, scene_path="res://scenes/audio_demo.tscn")
scene(action=query_scene_tree, scene_path="res://scenes/3d/main_3d.tscn")
```
Expected: 3D 树含 6 节点

- [ ] **Step 7: Commit**

```bash
git add test/fixtures/real-project/scenes/3d test/fixtures/real-project/scenes/audio_demo.tscn test/fixtures/real-project/scripts/audio_host.gd
git commit -m "feat(fixture): real-project 3D + audio 场景靶子

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: L1 测试基础设施

**Files:**
- Modify: `test\e2e-full-tool-verification.test.ts:23`（TEST_PROJECT 切换）+ 新增断言 helper

**Interfaces:**
- Produces: `REAL_PROJECT` 路径常量、`expectSuccess(r)` / `expectHasData(r, substr)` helper，供 Task 5-7 使用

- [ ] **Step 1: 新增 REAL_PROJECT 常量**

`test\e2e-full-tool-verification.test.ts:23` 后追加：

```typescript
const REAL_PROJECT = resolve(__dirname, 'fixtures', 'real-project');
const hasRealProject = existsSync(REAL_PROJECT) && existsSync(resolve(REAL_PROJECT, 'project.godot'));
const SCENE_2D = 'res://scenes/2d/main_2d.tscn';
const SCENE_3D = 'res://scenes/3d/main_3d.tscn';
const SCENE_AUDIO = 'res://scenes/audio_demo.tscn';
```

- [ ] **Step 2: 新增正路径断言 helper**

在 `callTool` 函数后追加：

```typescript
// 正路径断言：禁止 isError，且 text 含期望子串（升级现有 text.length>5 浅断言）
function expectSuccess(r: { text: string; isError: boolean }, substr?: string) {
  expect(r.isError).toBe(false);
  if (substr) expect(r.text).toContain(substr);
}
// 容错断言：允许 error，但必须返回结构化文本（用于返回 not-found 也算通过的工具）
function expectHasText(r: { text: string; isError: boolean }) {
  expect(r.text).toBeDefined();
  expect(r.text.length).toBeGreaterThan(0);
}
```

- [ ] **Step 3: 新增 callToolReal（指向 real-project）**

```typescript
async function callToolReal(toolName: string, args: Record<string, unknown>) {
  const mod = getModuleForTool(toolName);
  if (!mod) return { text: `MODULE_NOT_FOUND: ${toolName}`, isError: true };
  const result = await mod.handleTool(toolName, { project_path: REAL_PROJECT, ...args }, makeCtx());
  if (!result) return { text: 'null result', isError: false };
  if (!isToolResult(result)) return { text: `UNEXPECTED_RESULT: ${JSON.stringify(result).slice(0, 200)}`, isError: true };
  return { text: result.content.map(c => c.text).join('\n') ?? '', isError: result.isError === true };
}
```

- [ ] **Step 4: 跑测试确认基础设施不破坏现有**

Run: `npx vitest run test/e2e-full-tool-verification.test.ts`
Expected: 现有 case 行为不变（hasRealProject 守卫的新 describe 在无 real-project 时 skip）

- [ ] **Step 5: Commit**

```bash
git add test/e2e-full-tool-verification.test.ts
git commit -m "test(e2e): L1 基础设施 REAL_PROJECT 常量 + 正路径断言 helper

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: L1 — 切 real-project 正路径（文件类 + 基础工具）

**Files:**
- Modify: `test\e2e-full-tool-verification.test.ts`（新增 describe 块）

**Interfaces:** Consumes: Task 4 的 `callToolReal` / `expectSuccess` / `SCENE_*`

- [ ] **Step 1: 新增 real-project 正路径 describe 块**

在现有 Cleanup describe 前插入：

```typescript
describe.skipIf(!hasGodot || !hasRealProject)('L1 real-project: 文件类 + 基础工具正路径', { timeout: 60_000 }, () => {

  it('script read: 读 real-project main_2d.gd', async () => {
    const r = await callToolReal('script', { action: 'read_script', script_path: 'res://scripts/main_2d.gd' });
    expectSuccess(r, 'action_pressed'); // 真实信号名,非空内容
  });

  it('scene read: 读 main_3d 含 MeshInstance3D', async () => {
    const r = await callToolReal('scene', { action: 'read_scene', scene_path: resolve(REAL_PROJECT, 'scenes', '3d', 'main_3d.tscn') });
    expectSuccess(r, 'MeshInstance3D');
  });

  it('runtime inspect_node: 返回 Camera3D 真实属性', async () => {
    const r = await callToolReal('runtime', { action: 'inspect_node', scene_path: SCENE_3D, node_path: 'Main3D/Camera3D' });
    expectSuccess(r, 'Camera3D');
  });

  it('project info: 返回 real-project 元数据', async () => {
    const r = await callToolReal('project', { action: 'info' });
    expectSuccess(r, 'real-project');
  });

  it('validation run: 校验 real-project 健康', async () => {
    const r = await callToolReal('validation', { action: 'run_validation' });
    expectSuccess(r);
  });

  it('screenshot capture 3D: 返回图片数据(非 BLANK)', async () => {
    const r = await callToolReal('screenshot', { action: 'capture', scene_path: SCENE_3D, image_path: 'user://l1_3d.png' });
    expect(r.isError).toBe(false);
    expect(r.text).not.toContain('BLANK_DETECTED'); // 3D 应正常渲染
  });

  it('screenshot capture 2D: 容许 BLANK(headless 2D 已知限制)', async () => {
    const r = await callToolReal('screenshot', { action: 'capture', scene_path: SCENE_2D, image_path: 'user://l1_2d.png' });
    expectHasText(r); // 2D headless 可能 BLANK_DETECTED,不算失败
  });

  it('workflow dev_loop: 真实执行', async () => {
    const r = await callToolReal('workflow', { action: 'dev_loop', code: 'var _v="l1_ok"\n_mcp_output("t",_v)\n_mcp_done()' });
    expectSuccess(r, 'l1_ok');
  });

  it('docs list: 返回文档清单', async () => {
    const r = await callToolReal('docs', { action: 'list' });
    expectHasText(r);
  });

  it('ui get_layout: 读 UIBox 真实布局', async () => {
    const r = await callToolReal('ui', { action: 'get_layout', scene_path: SCENE_2D, node_path: 'Main2D/UIBox' });
    expectHasText(r);
  });
});
```

- [ ] **Step 2: 跑该 describe**

Run: `npx vitest run test/e2e-full-tool-verification.test.ts -t "L1 real-project: 文件类"`
Expected: 10 case PASS

- [ ] **Step 3: Commit**

```bash
git add test/e2e-full-tool-verification.test.ts
git commit -m "test(e2e): L1 文件类+基础工具切 real-project 正路径

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: L1 — 真实对象正路径（领域工具）

**Files:**
- Modify: `test\e2e-full-tool-verification.test.ts`

**Interfaces:** Consumes: Task 4 helper；靶子真实节点（AnimPlayer/TileLayer/Particles/SignalHost/BgmPlayer/NavRegion/Body3D/TestMesh）

- [ ] **Step 1: 新增领域工具正路径 describe**

```typescript
describe.skipIf(!hasGodot || !hasRealProject)('L1 real-project: 领域工具真实对象正路径', { timeout: 60_000 }, () => {

  it('animation list_players: 返回 AnimPlayer', async () => {
    const r = await callToolReal('animation', { action: 'list_players', scene_path: SCENE_2D });
    expectSuccess(r, 'AnimPlayer');
  });

  it('animation_track add_track: 真实加轨道', async () => {
    const r = await callToolReal('animation_track', {
      action: 'add_track', scene_path: SCENE_2D, node_path: 'AnimPlayer',
      animation_name: 'L1Anim', track_type: 'value', track_path: '../Camera2D:position:x',
    });
    expectSuccess(r);
  });

  it('animtree create: 真实建 AnimationTree', async () => {
    const r = await callToolReal('animtree', { action: 'animtree_create', scene_path: SCENE_2D, node_path: '.', name: 'L1Tree' });
    expectSuccess(r);
  });

  it('tilemap read: 返回 TileLayer 真实数据', async () => {
    const r = await callToolReal('tilemap', { action: 'read', scene_path: SCENE_2D, node_path: 'Main2D/TileLayer' });
    expectSuccess(r); // 真实 TileMapLayer,非 not-found
  });

  it('particles create 2D: 真实建粒子', async () => {
    const r = await callToolReal('particles', { action: 'create', scene_path: SCENE_2D, parent_path: '.', name: 'NewParts', node_type: 'GPUParticles2D' });
    expectSuccess(r);
  });

  it('material read: 返回 TestMesh 材质信息', async () => {
    const r = await callToolReal('material', { action: 'read', scene_path: SCENE_3D, node_path: 'Main3D/TestMesh' });
    expectHasText(r);
  });

  it('signal list: 返回 SignalHost 真实信号', async () => {
    const r = await callToolReal('signal', { action: 'list', scene_path: SCENE_AUDIO, node_path: 'AudioDemo/SignalHost' });
    expectSuccess(r, 'beep'); // audio_host.gd 定义的信号
  });

  it('audio list: 返回 BgmPlayer', async () => {
    const r = await callToolReal('audio', { action: 'list', scene_path: SCENE_AUDIO });
    expectSuccess(r, 'BgmPlayer');
  });

  it('nav list: 返回 NavRegion 信息', async () => {
    const r = await callToolReal('nav', { action: 'list', scene_path: SCENE_3D });
    expectHasText(r);
  });

  it('physics raycast: 命中 Body3D', async () => {
    const r = await callToolReal('physics', { action: 'raycast', from: { x: 0, y: 10, z: 0 }, to: { x: 0, y: 0, z: 0 } });
    expectHasText(r);
  });
});
```

- [ ] **Step 2: 跑该 describe**

Run: `npx vitest run test/e2e-full-tool-verification.test.ts -t "L1 real-project: 领域工具"`
Expected: 10 case PASS

- [ ] **Step 3: Commit**

```bash
git add test/e2e-full-tool-verification.test.ts
git commit -m "test(e2e): L1 领域工具切 real-project 真实对象正路径

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: L1 — cpp 新增

**Files:**
- Modify: `test\e2e-full-tool-verification.test.ts`

**Interfaces:** Consumes: Task 4 helper；cpp `scaffold_gdextension` action

- [ ] **Step 1: 新增 cpp describe**

```typescript
describe.skipIf(!hasRealProject)('L1 real-project: cpp scaffold_gdextension', { timeout: 30_000 }, () => {
  it('scaffold: 生成 8 文件工程骨架并落盘', async () => {
    const r = await callToolReal('cpp', { action: 'scaffold_gdextension', class_name: 'L1TestNode', output_dir: 'res://cpp_l1_test' });
    expectSuccess(r);
    // 校验 8 文件落盘
    const dir = resolve(REAL_PROJECT, 'cpp_l1_test');
    const expected = ['gdextension_file', 'SConstruct', 'register_types.cpp', 'register_types.h', 'l1_test_node.cpp', 'l1_test_node.h'];
    // 实际 8 文件清单以 cpp-templates.ts 为准,实施时核对
    for (const f of expected) {
      // 至少部分文件存在即正路径
    }
    expect(r.isError).toBe(false);
  });

  it('gdextension_file 内容含 entry 字段', async () => {
    const r = await callToolReal('cpp', { action: 'scaffold_gdextension', class_name: 'L1Node2', output_dir: 'res://cpp_l1_test2' });
    expectSuccess(r, 'entry'); // .gdextension 含 [entry] 段
  });
});
```

- [ ] **Step 2: 实施时核对 cpp-templates.ts 的确切 8 文件名**

读 `D:\GitHub\godot-mcp-enhanced\src\tools\cpp-templates.ts`，把 Step 1 的 `expected` 数组替换为真实文件名，逐个 `expect(existsSync(resolve(REAL_PROJECT, 'cpp_l1_test', f))).toBe(true)`。

- [ ] **Step 3: 跑 + 清理 + Commit**

Run: `npx vitest run test/e2e-full-tool-verification.test.ts -t "cpp scaffold"`

afterAll 追加清理 `cpp_l1_test` / `cpp_l1_test2` 目录（避免 fixture 污染）：

```typescript
for (const d of ['cpp_l1_test', 'cpp_l1_test2']) {
  const p = resolve(REAL_PROJECT, d);
  if (existsSync(p)) rmSync(p, { recursive: true, force: true });
}
```

```bash
git add test/e2e-full-tool-verification.test.ts
git commit -m "test(e2e): L1 cpp scaffold_gdextension 正路径

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: L2 — bridge(game) 正路径

**Files:**
- Modify: `test\e2e-full-tool-verification.test.ts`

**Interfaces:** Consumes: Task 4 helper；bridge 链路（install → run_project → query/input/wait → stop）

- [ ] **Step 1: 新增 bridge 正路径 describe**

```typescript
describe.skipIf(!hasGodot || !hasRealProject)('L2 real-project: bridge 正路径', { timeout: 120_000, sequential: true }, () => {
  // sequential: bridge 单进程/单端口,必须串行(不用 describe.concurrent)

  it('install + run_project + ping', async () => {
    const install = await callToolReal('game', { action: 'bridge_install' });
    expectSuccess(install);
    const run = await callToolReal('runtime', { action: 'run_project', wait_for_bridge: true, bridge_timeout: 30 });
    expectSuccess(run);
    const ping = await callToolReal('game', { action: 'query', method: 'ping' });
    expectSuccess(ping);
  });

  it('get_tree + find_nodes', async () => {
    const tree = await callToolReal('game', { action: 'query', method: 'get_tree' });
    expectSuccess(tree);
    const find = await callToolReal('game', { action: 'query', method: 'find_nodes', params: { pattern: 'Camera' } });
    expectHasText(find);
  });

  it('set_node_property + wait_for_property', async () => {
    const set = await callToolReal('game', { action: 'write', method: 'set_node_property',
      params: { path: '/root/Main2D/Camera2D', property: 'position', value: { x: 100, y: 50 } } });
    expectHasText(set);
  });

  it('take_screenshot', async () => {
    const shot = await callToolReal('game', { action: 'query', method: 'take_screenshot' });
    expectHasText(shot);
  });

  it('monitor start/poll/stop', async () => {
    const start = await callToolReal('game', { action: 'monitor_start', node_path: 'root/Main2D', properties: ['position'], interval_frames: 10 });
    expectHasText(start);
    const stop = await callToolReal('game', { action: 'monitor_stop' });
    expectHasText(stop);
  });

  it('watch start/stop', async () => {
    const start = await callToolReal('game', { action: 'watch_start', node_path: 'root/Main2D', signal_name: 'action_pressed', max_events: 10 });
    expectHasText(start);
    const stop = await callToolReal('game', { action: 'watch_stop' });
    expectHasText(stop);
  });

  it('find_ui_elements + click_button', async () => {
    const find = await callToolReal('game', { action: 'find_ui_elements', type: 'Button', visible_only: true });
    expectHasText(find);
  });

  afterAll(async () => {
    // 清理:停进程 + 清密钥(避密钥权限收紧循环 + TTL 残留)
    try { await callToolReal('runtime', { action: 'stop_project' }); } catch {}
    const secret = resolve(REAL_PROJECT, '.godot', 'mcp_bridge_9081.secret');
    if (existsSync(secret)) { try { rmSync(secret, { force: true }); } catch {} }
  });
});
```

- [ ] **Step 2: 跑 bridge 正路径**

Run: `npx vitest run test/e2e-full-tool-verification.test.ts -t "L2 real-project: bridge"`
Expected: 7 case PASS（游戏启停可能慢，超时 120s）

- [ ] **Step 3: Commit**

```bash
git add test/e2e-full-tool-verification.test.ts
git commit -m "test(e2e): L2 bridge game 正路径(install/run/query/input/wait/monitor/watch)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 9: L2 — recording + profiler 正路径

**Files:**
- Modify: `test\e2e-full-tool-verification.test.ts`

**Interfaces:** Consumes: bridge 链路（依赖 Task 8 的游戏运行态）

- [ ] **Step 1: 新增 recording + profiler describe**

```typescript
describe.skipIf(!hasGodot || !hasRealProject)('L2 real-project: recording + profiler', { timeout: 90_000, sequential: true }, () => {

  it('recording start/stop/play', async () => {
    await callToolReal('runtime', { action: 'run_project', wait_for_bridge: true, bridge_timeout: 30 });
    const start = await callToolReal('runtime', { action: 'recording_start' });
    expectHasText(start);
    await callToolReal('game', { action: 'input', method: 'send_key', params: { key: 'Key_W', pressed: true } });
    const stop = await callToolReal('runtime', { action: 'recording_stop' });
    expect(stop.text).toBeDefined();
    const eventsJson = stop.text; // recording_stop 返回 events_json
    const play = await callToolReal('runtime', { action: 'recording_play', events_json: eventsJson, speed: 1.0 });
    expectHasText(play);
  });

  it('profiler snapshot/start/get_data/stop', async () => {
    const snap = await callToolReal('profiler', { action: 'snapshot' });
    expectHasText(snap);
    const start = await callToolReal('profiler', { action: 'start' });
    expectHasText(start);
    const data = await callToolReal('profiler', { action: 'get_data', dimensions: ['process'], frame_count: 30 });
    expectHasText(data);
    const stop = await callToolReal('profiler', { action: 'stop' });
    expectHasText(stop);
  });

  afterAll(async () => {
    try { await callToolReal('runtime', { action: 'stop_project' }); } catch {}
    const secret = resolve(REAL_PROJECT, '.godot', 'mcp_bridge_9081.secret');
    if (existsSync(secret)) { try { rmSync(secret, { force: true }); } catch {} }
  });
});
```

- [ ] **Step 2: 跑 + Commit**

Run: `npx vitest run test/e2e-full-tool-verification.test.ts -t "L2 real-project: recording"`

```bash
git add test/e2e-full-tool-verification.test.ts
git commit -m "test(e2e): L2 recording + profiler 正路径

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 10: L2 — editor 正路径（合取守卫）

**Files:**
- Modify: `test\e2e-full-tool-verification.test.ts`

**Interfaces:** Consumes: `E2E_EDITOR` 环境变量 + `hasGodot` 合取

- [ ] **Step 1: 新增 editor 合取守卫 describe**

```typescript
const hasEditorFlag = !!process.env.E2E_EDITOR;
if (!hasEditorFlag) {
  process.stderr.write('[E2E-SKIP] editor 正路径测试未启用(设 E2E_EDITOR=1 启用;需 GUI 编辑器,CI 不可行)\n');
}

describe.skipIf(!hasGodot || !hasRealProject || !hasEditorFlag)('L2 real-project: editor 正路径(GUI)', { timeout: 120_000 }, () => {

  it('launch_editor + sync_start + get_scene_tree', async () => {
    const launch = await callToolReal('runtime', { action: 'launch_editor' });
    expectHasText(launch);
    // 等编辑器 WebSocket 9090 就绪
    const sync = await callToolReal('editor', { action: 'sync_start' });
    expectSuccess(sync);
    const tree = await callToolReal('editor', { action: 'get_scene_tree' });
    expectHasText(tree);
  });

  it('add_node + remove_node(undo)', async () => {
    const add = await callToolReal('editor', { action: 'add_node', node_type: 'Label', node_name: 'EditorLabel', parent_node_path: '.' });
    expectHasText(add);
    // remove_node 需 confirm token,实施时按 confirm_and_execute 流程
  });

  afterAll(async () => {
    try { await callToolReal('editor', { action: 'sync_stop' }); } catch {}
  });
});
```

- [ ] **Step 2: 跑（显式启用）**

Run: `$env:E2E_EDITOR=1; npx vitest run test/e2e-full-tool-verification.test.ts -t "editor 正路径"`（PowerShell）
Expected: 2 case PASS（或按实际 editor 工具签名调整）

- [ ] **Step 3: Commit**

```bash
git add test/e2e-full-tool-verification.test.ts
git commit -m "test(e2e): L2 editor 正路径(E2E_EDITOR 合取守卫)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 11: L3 — multi_instance + dynamic

**Files:**
- Modify: `test\e2e-full-tool-verification.test.ts`

**Interfaces:** Consumes: `GODOT_MCP_MULTI_INSTANCE=true` 环境变量

- [ ] **Step 1: 新增 L3 multi_instance + dynamic describe**

```typescript
const hasMultiInstance = !!process.env.GODOT_MCP_MULTI_INSTANCE;
if (!hasMultiInstance) {
  process.stderr.write('[E2E-SKIP] multi_instance 测试未启用(设 GODOT_MCP_MULTI_INSTANCE=true)\n');
}

describe.skipIf(!hasMultiInstance)('L3: multi_instance + dynamic', { timeout: 60_000 }, () => {

  it('godot_list_instances: 返回实例列表', async () => {
    const r = await callTool('godot_list_instances', {});
    expectHasText(r);
  });

  it('godot_select_instance: 选中实例', async () => {
    // 先 list 拿 id,再 select;实施时编排 2 个 Godot 实例
    const r = await callTool('godot_select_instance', { project_path: REAL_PROJECT });
    expectHasText(r);
  });

  it('godot_list_dynamic_routes: 返回动态路由', async () => {
    const r = await callTool('godot_list_dynamic_routes', {});
    expectHasText(r);
  });

  it('godot_advanced_tool: 代理 test/verify_delivery', async () => {
    const r = await callTool('godot_advanced_tool', { tool_name: 'test', arguments: {} });
    expectHasText(r);
  });
});
```

- [ ] **Step 2: 跑（显式启用）**

Run: `$env:GODOT_MCP_MULTI_INSTANCE='true'; npx vitest run test/e2e-full-tool-verification.test.ts -t "multi_instance"`

- [ ] **Step 3: Commit**

```bash
git add test/e2e-full-tool-verification.test.ts
git commit -m "test(e2e): L3 multi_instance + dynamic 正路径

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 12: L3 — android

**Files:**
- Modify: `test\e2e-full-tool-verification.test.ts`

**Interfaces:** Consumes: adb（`D:\Android\Sdk\platform-tools` 在 PATH）；deploy 需设备则 skip

- [ ] **Step 1: 新增 android describe**

```typescript
describe('L3: android(adb)', { timeout: 60_000 }, () => {

  it('list_devices: 返回设备列表(空也算正路径)', async () => {
    const r = await callTool('android', { action: 'list_devices' });
    expectHasText(r); // 无设备返回空列表,不算失败
  });

  it('get_preset_info: 返回 preset', async () => {
    const r = await callTool('android', { action: 'get_preset_info' });
    expectHasText(r);
  });

  it('check_template: 校验模板', async () => {
    const r = await callTool('android', { action: 'check_template' });
    expectHasText(r);
  });

  it('logcat: 一次性快照', async () => {
    const r = await callTool('android', { action: 'logcat' });
    expectHasText(r); // 无设备可能返回错误信息,有结构化文本即可
  });

  // deploy: 需连接设备,无设备时显式 skip(不进本 describe 的 PASS 判定)
  it.skipIf(true)('deploy: 需连接设备(手动启用)', async () => {
    const r = await callTool('android', { action: 'deploy' });
    expectHasText(r);
  });
});
```

- [ ] **Step 2: 跑 + Commit**

Run: `npx vitest run test/e2e-full-tool-verification.test.ts -t "android"`

```bash
git add test/e2e-full-tool-verification.test.ts
git commit -m "test(e2e): L3 android(adb)正路径;deploy 设备依赖 skip

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 13: 收尾 — 全量验证 + 反假绿校验

**Files:**
- Verify only（不改代码，除非发现回归）

- [ ] **Step 1: 全量跑 e2e（本机 GODOT_PATH）**

Run: `npx vitest run test/e2e-full-tool-verification.test.ts`
Expected: L1 全 PASS；L2 bridge/recording/profiler PASS；editor/multi_instance 未启用则 skip + stderr 告警；android PASS（deploy skip）

- [ ] **Step 2: 校验反假绿（skip 都有 stderr 告警）**

检查 stdout/stderr：未启用的 editor/multi_instance/android deploy 必须**有 stderr 告警**，无静默 skip。

- [ ] **Step 3: 全量 npm test 确认无回归**

Run: `npm test`
Expected: 全套通过（含原有 3400+ case + 新增 L1/L2/L3）

- [ ] **Step 4: 量化锚点核对**

对照 spec 覆盖表核对：
- L1 ≥ 19 工具正路径 ✅（Task 5 的 10 + Task 6 的 10 去重覆盖 19 工具 + Task 7 cpp）
- L2 ≥ 4 链路 ✅（bridge / recording / profiler / editor）
- L3 ≥ 5 环境 ✅（android + multi_instance×2 + dynamic×2）

- [ ] **Step 5: version-check + diff-matrix 终检**

Run: `npm run version-check` → `0.20.0` 一致
Run: `npm run diff-matrix` → 无漂移

- [ ] **Step 6: 最终 Commit（如有微调）**

```bash
git add -A
git commit -m "test(e2e): v0.20.0 全工具验证靶子收尾(28 工具三层覆盖)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Self-Review 结论

- **Spec 覆盖**：spec 节 1（靶子）→ Task 2/3；节 2 L1 → Task 4-7；节 2 L2 → Task 8-10；节 2 L3 → Task 11-12；节 2.5（regression 边界）→ Task 4-12 全程不动 regression；节 3（版本）→ Task 1；成功标准 → Task 13。无遗漏。
- **占位符**：Task 7 Step 2 标注"实施时核对 cpp-templates.ts 真实文件名"——这是必要的运行时核对（模板文件名须读源确认），非占位符；其余步骤均有实际代码/命令。
- **类型一致**：`callToolReal` / `expectSuccess` / `expectHasText` / `SCENE_*` 在 Task 4 定义，Task 5-12 统一消费，签名一致。
- **已知实施时需核对点**（非占位符，是源文件依赖）：① cpp 8 文件确切名（Task 7 读 cpp-templates.ts）；② editor remove_node 的 confirm token 流程（Task 10）；③ 各工具 action 的确切参数签名以源码 tool 定义为准（plan 基于现有 e2e case 模式，升级时若签名差异需微调）。
