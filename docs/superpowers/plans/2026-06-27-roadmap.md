# ROADMAP.md 创建与归并 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development(推荐)或 superpowers:executing-plans 逐 task 实现。步骤用 `- [ ]` 跟踪。

**Goal:** 创建项目级 `ROADMAP.md` 路线图,按方案 A 单分支闭环归并到 `feat/roadmap`,与 README+LICENSE(`640020b`)一次性合并 master。

**Architecture:** 在隔离 git worktree(主 worktree 有并发 recording session)挂 `feat/roadmap` 分支 → cherry-pick `docs/roadmap` 的 ROADMAP spec → 创建 `ROADMAP.md` → 补 README/spec 链接 → 走 finishing-a-development-branch 合并 master。

**Tech Stack:** Markdown 文档 + git worktree / cherry-pick / merge。

## Global Constraints

- **隔离 worktree 操作**:主 worktree(`D:\GitHub\godot-mcp-enhanced`)有并发 recording session(`feat/recording-screen-drag`),所有 `feat/roadmap` 操作**必须**在隔离 worktree,避免互相切分支干扰(spec 丢失的根因)。
- **方案 A 单分支闭环**:ROADMAP 产物全部归 `feat/roadmap`,与 README+LICENSE(`640020b`)一次性合并 master,无中间态。
- **绝对路径**引用文件(项目规则)。
- **中文**文档(项目规则)。
- **合并 master 走 `superpowers:finishing-a-development-branch`**(发版级,需 `verify_delivery` + 用户确认)。
- spec 来源:`D:\GitHub\godot-mcp-enhanced\docs\superpowers\specs\2026-06-27-roadmap-design.md`(r2 + N1)。

---

### Task 1: 隔离 worktree 挂 feat/roadmap + 合入 ROADMAP spec

**Files:**
- worktree: `D:\GitHub\godot-mcp-enhanced\.claude\worktrees\roadmap-merge`
- branch: `feat/roadmap`

**Interfaces:**
- Consumes: `docs/roadmap` 分支的 `265a612`(初版 spec)+ `8e42ba2`(补 N1)
- Produces: `feat/roadmap` 分支含 ROADMAP spec(供 Task 2/3 引用)

- [ ] **Step 1: 创建 worktree 挂 feat/roadmap**

Run: `git worktree add .claude/worktrees/roadmap-merge feat/roadmap`
Expected: `Preparing worktree (checking out 'feat/roadmap')` + `HEAD is now at 640020b`

- [ ] **Step 2: cherry-pick ROADMAP spec 的两个 commit**

Run: `git -C .claude/worktrees/roadmap-merge cherry-pick 265a612 8e42ba2`
Expected: 两个 commit 干净应用(两分支都基于 `e6143c2`,spec 文件路径 `docs/superpowers/specs/2026-06-27-roadmap-design.md` 不与 feat/roadmap 文件冲突)。若冲突,仅在 spec 文件内,保留 8e42ba2 的 N1 版本。

- [ ] **Step 3: 验证 spec 在 feat/roadmap**

Run: `git -C .claude/worktrees/roadmap-merge log --oneline -4`
Expected: 顶部为 `8e42ba2` 的 cherry-pick(新 SHA),其次 `265a612` cherry-pick,再 `640020b`,`e6143c2`。

Run: `git -C .claude/worktrees/roadmap-merge show HEAD:docs/superpowers/specs/2026-06-27-roadmap-design.md | head -1`
Expected: `# ROADMAP.md 路线图设计(2026-06-27)`

> cherry-pick 自带 commit,无需额外提交。

---

### Task 2: 创建 ROADMAP.md

**Files:**
- Create: `D:\GitHub\godot-mcp-enhanced\.claude\worktrees\roadmap-merge\ROADMAP.md`

**Interfaces:**
- Produces: `ROADMAP.md`(项目根路线图,被 README.md 链接 —— Task 3)

- [ ] **Step 1: 创建 ROADMAP.md(完整内容如下)**

````markdown
# Roadmap

> 免费 · 开源 · 安全 · 三层架构 —— 持续深化 Godot MCP 的差异化定位。
> 本路线图是规划意图,非交付承诺;状态随开发推进更新。版本号为目标参考。

## 状态图例

| 图标 | 含义 |
|---|---|
| ✅ | 已完成 |
| 🟡 | 进行中(spec 就绪 / 实现中 / 部分完成) |
| 📋 | 已规划 |
| 💤 | 考虑中 / 搁置(可 reopen) |

## M1 — 定位与声量(不绑版本,随 ready 发)

主题:**让赛道看见我们**。文档与运营工作。prior session 已完成核心(#1/#2),剩 CodeBuddy 分发(#10/#11)与 README.en 完整重构(#12)。

| # | 行动项 | 状态 | 关联 |
|---|---|---|---|
| 1 | 补 LICENSE(双版权 MIT) | ✅ | feat/roadmap `640020b`(待合并 master) |
| 2 | README.md 重定位改写 | ✅ | feat/roadmap `640020b`(待合并 master);spec: 2026-06-27-readme-repositioning-design.md |
| 12 | README.en 完整双语重构 | 🟡 | `640020b` 已加滞后注,完整重构待做 |
| 11 | CodeBuddy 端到端接入验证 | 📋 | 验证通过解锁 #10 |
| 10 | 上架 CodeBuddy MCP Market | 📋 | 依赖 #11 验证通过 |

> #11 → #10 顺序硬约束:验证未通过不上架。#10 上架后,README Hero 兼容列表才可移入 CodeBuddy。

<details><summary>#11 CodeBuddy 端到端接入验证 — 详情</summary>

- **目标**:在 CodeBuddy 内跑通 godot-mcp-enhanced,验证 stdio MCP 接入
- **来源**:战略文档第八节「CodeBuddy = MCP client 可借力分发」
- **验收**:CodeBuddy 配置本项目后,至少 1 个工具(如 read_scene)端到端调用成功;通过后解锁 #10 上架 + README Hero 兼容列表移入
- **依赖**:无(但 #10 上架依赖本项通过)
</details>

## M2 — 健壮性 P0(目标 v0.20)

主题:**让 agent 少踩坑**。源码深挖 P0 三项,直击 execute_gdscript 非确定 + agent 错误自愈。

| # | 行动项 | 状态 | 关联 |
|---|---|---|---|
| 4 | 错误返回 suggestion 字段 | 📋 | spec 待写 |
| 3 | icon 匹配确定性 UI 检测 | 📋 | spec 待写 |
| 5 | timeout 分层诊断 | 📋 | spec 待写 |

<details><summary>#3 icon 匹配 — ⚠️ 已撤销(2026-06-28 核实)</summary>

- **撤销原因**:`execute_gdscript` 是 headless 独立进程,不操作编辑器 UI;"看板/select 检测"全仓 0 代码命中(把竞品 editor addon 检测 debugger Continue 按钮的 icon 匹配,错套到 headless execute_gdscript)。已从 ROADMAP.md 撤销;#5 timeout 改归 Bridge(对标 `game-bridge.ts:733` 缺口)
- ~~**目标**(已撤销)~~:execute_gdscript 看板/select 检测从 UI 文本匹配迁到 EditorIcons theme icon 匹配,根治非中文/英文 Godot 编辑器下失效
- **来源**:godot-mcp-pro `base_command.gd:261-290`(issue #34 意大利语「Continua ≠ Continue」教训)
- **验收**:非英文 Godot 编辑器下看板/select 检测稳定;新增 locale 适配测试
- **依赖**:无
</details>

## M3 — 安全 P1(目标 v0.21)

主题:**守住安全差异化**。独立里程碑强化「安全维度赛道空白」叙事。

| # | 行动项 | 状态 | 关联 |
|---|---|---|---|
| 6 | 编辑器打开场景/脚本写入 guard | 📋 | spec 待写 |

## M4 — 功能补齐 P2(目标 v0.22+,部分 💤)

主题:**补齐竞品已占能力**。三项均 💤 考虑中,非近期承诺。

| # | 行动项 | 状态 | 关联 |
|---|---|---|---|
| 8 | profiling_commands 补齐 | 💤 | — |
| 9 | UndoRedo 封装完善 | 💤 | — |
| 7 | Android Deploy / 导出模板校验 | 💤 | 社区痛点「能装不能跑」 |

## 明确不做什么

- **不拼工具数量**:工具数不是卖点(不跟 godot-mcp-pro 的 175);README 对比表诚实列出工具数但不以此竞争,以「免费+开源+安全+三层」差异化
- **不做闭源付费**:坚守免费开源定位(赛道真空地带)
- **不承诺 P2 时间**:M4 三项(Android/profiling/UndoRedo)标 💤 考虑中,非近期承诺

## 路线图变更记录

> 本节只记里程碑/状态变更(📋→🟡→✅ + 日期)。版本功能详见 CHANGELOG.md。

- 2026-06-27 — 初版路线图发布(M1–M4 + 12 行动项);M1 #1/#2 已完成(feat/roadmap `640020b`)
````

- [ ] **Step 2: 验证结构**

Run: `grep -c "^## M" .claude/worktrees/roadmap-merge/ROADMAP.md`
Expected: `4`(M1/M2/M3/M4)

Run: `grep -c "^|" .claude/worktrees/roadmap-merge/ROADMAP.md`
Expected: ≥ `20`(4 表格的行)

- [ ] **Step 3: commit**

Run:
```bash
git -C .claude/worktrees/roadmap-merge add ROADMAP.md
git -C .claude/worktrees/roadmap-merge commit -m "docs(roadmap): 创建项目级 ROADMAP.md 路线图(M1-M4 + 12 行动项)" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: README.md + readme r2 spec 加 Roadmap 链接

**Files:**
- Modify: `D:\GitHub\godot-mcp-enhanced\.claude\worktrees\roadmap-merge\README.md`(收尾/许可证节)
- Modify: `D:\GitHub\godot-mcp-enhanced\.claude\worktrees\roadmap-merge\docs\superpowers\specs\2026-06-27-readme-repositioning-design.md`(第 7 节收尾)

**Interfaces:**
- Consumes: Task 2 的 `ROADMAP.md`(链接目标)
- Produces: README.md 含 `[Roadmap](ROADMAP.md)` 链接(访客可见,N4 收紧)

- [ ] **Step 1: 定位 README.md 收尾节**

Run: `grep -n "许可证\|LICENSE\|## 收尾\|更新日志" .claude/worktrees/roadmap-merge/README.md`
Expected: 找到收尾节标题行号(README r2 重定位版有"收尾"节含 LICENSE/致谢/更新日志/系统要求)

- [ ] **Step 2: README.md 收尾节加 Roadmap 链接**

在收尾节(许可证/致谢附近)加一行:
```markdown
- **[路线图](ROADMAP.md)**:已规划方向与里程碑(M1 定位与声量 / M2 健壮性 P0 / M3 安全 P1 / M4 功能补齐 P2)
```
用 `edit_script` 的 `search_and_replace`(CRLF 安全)或 Edit(.md 文件可用内置 Edit)。old_string 用 Step 1 定位的收尾节锚点(如"## 收尾"或"- **许可证**"行),new_string 在其后插入 Roadmap 行。

- [ ] **Step 3: 回改 readme r2 spec 第 7 节**

`2026-06-27-readme-repositioning-design.md` 第 7 节"收尾"的清单(含 LICENSE/致谢/更新日志/系统要求/截图)补一条:
```markdown
- **路线图**:加 `[Roadmap](ROADMAP.md)` 链接(对应项目根 ROADMAP.md)
```
定位:该 spec 第 7 节"### 7. 收尾"下方的列表。

- [ ] **Step 4: 验证链接存在**

Run: `grep -rn "ROADMAP.md" .claude/worktrees/roadmap-merge/README.md .claude/worktrees/roadmap-merge/docs/superpowers/specs/2026-06-27-readme-repositioning-design.md`
Expected: 至少 2 行命中(README + spec 各含链接)

- [ ] **Step 5: commit**

Run:
```bash
git -C .claude/worktrees/roadmap-merge add README.md docs/superpowers/specs/2026-06-27-readme-repositioning-design.md
git -C .claude/worktrees/roadmap-merge commit -m "docs(roadmap): README + readme r2 spec 加 Roadmap 链接(I5)" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: 收尾 —— feat/roadmap 合并 master

**Files:**
- 整分支:`feat/roadmap` → `master`

- [ ] **Step 1: 走 `superpowers:finishing-a-development-branch`**

合并 `feat/roadmap`(含 README 重定位 + LICENSE + ROADMAP.md + spec 回改)到 `master`。这是发版级操作(README/LICENSE 落地主线 + ROADMAP 对外公开),按 finishing-a-development-branch 流程:
- 确认 feat/roadmap 测试绿(`verify_delivery` + `validate_scripts`)
- 创建 PR 或直接合并(用户确认)
- 合并后 master 含 README 重定位 + LICENSE + ROADMAP.md(M1「让赛道看见我们」对外落地)

- [ ] **Step 2: 合并后清理 worktree**

Run:
```bash
git worktree remove .claude/worktrees/roadmap-merge
git worktree remove .claude/worktrees/roadmap  # docs/roadmap(本 spec),已合入 feat/roadmap
git branch -d docs/roadmap  # 如已合入,删除临时分支
```
Expected: worktree 移除,临时分支删除(feat/roadmap 保留至合并 master 后)。

- [ ] **Step 3: 验证 master 含 ROADMAP.md + README 重定位**

Run: `git show master:ROADMAP.md | head -1` → Expected: `# Roadmap`
Run: `git show master:README.md | head -3` → Expected: 重定位版 Hero(「免费 · 开源 · 全功能」)

---

## Self-Review

**1. Spec coverage:**
- 创建 ROADMAP.md → Task 2 ✓
- 回改 readme r2 spec 第7节(I5)→ Task 3 Step 3 ✓
- README 加链接 → Task 3 Step 2 ✓
- 方案 A 单分支归并 → Task 1(cherry-pick)+ Task 4(合并)✓
- #1/#2 ✅ 状态、#12 🟡 状态 → Task 2 ROADMAP.md 内容 ✓
- 状态机 4 态 / 详情 4 字段 → Task 2 内容 ✓
- N2(M2 排序)/N3(#7 出处)/N4(README 链接收紧)→ N4 在 Task 3(README 链接访客可见);N2/N3 是 ROADMAP 内容微调,执行 Task 2 时按需补(排序依据/#7 出处占位)

**2. Placeholder scan:** Task 3 Step 2/3 用"定位锚点 + 插入内容"模式(执行者读真实文件定位),非 placeholder——给了明确位置与插入内容。ROADMAP.md 完整内容在 Task 2(无 TODO/TBD)。

**3. Type consistency:** 文件路径一致(`ROADMAP.md` 项目根;spec 路径统一绝对/相对);分支名一致(`feat/roadmap` / `docs/roadmap` / `master`)。

**执行前提(必须):**
- 并发 recording session 在主 worktree → 所有操作在隔离 worktree(`.claude/worktrees/roadmap-merge`)
- Task 4 合并 master 前确认并发 session 已协调(避免合并冲突)
