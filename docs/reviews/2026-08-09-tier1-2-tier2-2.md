# 第三方审查:Tier1-2 scene_editing_strategy prompt + Tier2-2 tscn parser 补 4 类型

> 审查日期: 2026-08-09
> 审查者: code-reviewer 子 agent(隔离视角)
> 审查对象: 分支 `feat/tier1-2-tier2-2-prompt-and-tscn`(未 commit,工作树改动)
> 审查方法: 所有声明用 grep/read 实测核实,未照搬作者声明

---

## 总体判定: **SHIPPED WITH NITS**

改动整体质量高,核心逻辑(tscn 4 类型 regex、prompt 三层结构、通用 handler 暴露、测试断言强度)均经实测确认正确。审查发现 1 个 Nit 级问题(prompt 正文工具名 `error_analyzer` 与实际 `analyze_error` 不一致),不影响核心功能但有误导 AI 调用的风险。**作者已当场修复 N-1**(2026-08-09 17:17,`src/prompts.ts:136` 改为 `analyze_error`),重跑 prompts 测试 20/20 全绿。N-2(tscn 单测未覆盖真实 fixture 文件)为可选改进项,留 follow-up。

---

## 审查范围

**审查文件**(4 个改动文件 + 6 个关联文件实测):
- `D:\GitHub\godot-mcp-enhanced\src\tscn\tscn-parser.ts`(parseValue 加 4 分支)
- `D:\GitHub\godot-mcp-enhanced\src\prompts.ts`(PROMPTS record 加 scene_editing_strategy)
- `D:\GitHub\godot-mcp-enhanced\test\prompts.test.ts`(硬编码 4→5 + 加 3 测试)
- `D:\GitHub\godot-mcp-enhanced\test\tscn-parser.test.js`(加 4 类型专项测试)

**关联实测文件**:
- `D:\GitHub\godot-mcp-enhanced\src\GodotServer.ts`(handler 通用性)
- `D:\GitHub\godot-mcp-enhanced\src\tools\get-context.ts`(workflows 自动暴露)
- `D:\GitHub\godot-mcp-enhanced\src\tools\scene\helpers.ts`(operation_type 枚举核实)
- `D:\GitHub\godot-mcp-enhanced\src\tools\validation.ts`(工具名核实)
- `D:\GitHub\godot-mcp-enhanced\test\e2e-scene\scenes\test_3d.tscn`(Transform3D 真实样本)
- `D:\GitHub\godot-mcp-enhanced\src\tools\rule-templates.ts` + `D:\GitHub\godot-mcp-enhanced\.claude\rules\`(独立副本边界核查)

---

## 逐维度结论

### 1. 设计正确性 — **通过**

**tscn 4 类型 regex 与分支顺序**(`src\tscn\tscn-parser.ts:140-188`):

实测确认 `^Vector2\(`(:141)与 `^Vector2i\(`(:157)互不误匹配 —— 正则 `^Vector2\(` 要求 `Vector2` 后紧跟 `(`,而 `Vector2i(...)` 第 8 个字符是 `i` 而非 `(`,故不会落入 Vector2 分支。注释 :156 已正确说明此点。Vector3/Vector3i 同理(:149 vs :165)。

**分支顺序正确性**:4 个新分支(Vector2i/Vector3i/Transform3D/PackedInt32Array)加在 Vector3 分支(:149-154)之后、Number 分支(:192-195)之前。没有任何新类型会被 Number 分支抢走,因为它们都以 `TypeName(` 开头,在 Number 之前就被拦截。Number 分支的 `trimmed !== ''` 守卫(:192)也不受影响。

**operation_type 枚举与真实 action 对应**(`src\prompts.ts:96`):枚举值 `['add_node', 'edit_node', 'remove_node', 'create_scene', 'read_scene']` 全部在 `src\tools\scene\helpers.ts:9-17` 的 `ACTIONS` 列表中核实存在。**5 个值都是真实有效的 scene action**。

**prompt 三层结构完整性**(`src\prompts.ts:103-151`):第 0 层(能力发现 / 连接模式探测)、第 1 层(操作决策树 / 5 分支)、第 2 层(闭环验证 / 截图+断言+失败处理)三层齐全,加上"关键陷阱"和"验证清单"两个收尾段。结构完整。

### 2. TS-GD 一致性 — **通过**

本次改动仅 TS 侧(无 .gd 改动)。Transform3D 真实样本核实:`test\e2e-scene\scenes\test_3d.tscn:36` 真实存在 `transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 2, 5)`,与 `src\tscn\tscn-parser.ts:173` 注释引用的样本完全一致。regex `^Transform3D\(`(:174)能正确匹配该格式。

**N-2 注意点(非阻断)**:虽然 test_3d.tscn 有真实 Transform3D 样本,但 `test\tscn-parser.test.js` 的测试用 inline 字符串(:181-188),未用真实 fixture 文件。这不构成测试缺陷(inline 测试更明确),但意味着 test_3d.tscn 的真实样本从未被 parseTscn 实际解析过 —— 若将来该文件格式有微妙差异(如多空格),单测抓不到。可选改进:加一个读真实文件的集成测试。

### 3. 测试质量 — **通过**

**tscn 4 类型测试**(`test\tscn-parser.test.js:180-199`):使用 `toEqual({ __type: 'Vector2i', value: '1, 2' })` 精确匹配,**同时验证了 `__type` 和 `value` 两个字段**,是强断言。4 个类型(Vector2i/Vector3i/PackedInt32Array/Transform3D)各有一条精确断言。

**prompts 测试**(`test\prompts.test.ts`):
- :16-24 `toHaveLength(5)` + 5 个 `toContain` 名称 —— 强断言,已正确从 4 更新到 5。
- :50-57 scene_editing_strategy 内容测试:`toContain('add_node')` + `toContain('能力发现')` + `toContain('决策树')` + `toContain('闭环验证')`。覆盖了三层结构关键词。
- :148-157 completion 测试:`toEqual(['add_node', 'edit_node', 'remove_node', 'create_scene', 'read_scene'])` + `toBe(5)` + `toBe(false)` —— 强断言,精确验证 enum。

**硬编码 4→5 完整性**:全局 grep `toHaveLength\(4\)` 仅命中 `test\scoring\generate-score.test.ts:58,74` 和 `test\types-middleware.test.ts:49`,均与 prompt 数量无关。`test\tools\get-context.test.ts:65` 对 workflows 只断言 `Array.isArray`,不硬编码数量。**无遗漏**。

**listPromptDefs 测试**(:69)用 `toBeGreaterThanOrEqual(5)`,这是通用"≥"语义,正确(未来再加 prompt 不破坏测试)。

### 4. 部署同步 — **通过**

**handler 通用性**(`src\GodotServer.ts:256-273`):三个 handler 完全通用 —— `prompts/list`(:256-258)调 `listPrompts()`、`prompts/get`(:260-263)调 `getPrompt(name, args)`、`completion/complete`(:266-273)调 `handleCompletion(ref, argument, projectPath)`。新 prompt 通过 `PROMPTS` record 注册后自动暴露,**无需改 handler**。作者声明可信。

**get-context workflows 自动暴露**(`src\tools\get-context.ts:14,96-98`):`listPromptDefs().map(p => ({ name, type: 'prompt', desc }))` 自动遍历所有注册 prompt,新 prompt 自动出现在 `godot_get_context` 的 `workflows` 字段。作者声明可信。

**capability-matrix 无需重建**:`src\capability\` 下 grep prompt 名(5 个)零命中。capability-matrix 跟踪的是 **工具**(tools),不是 prompts。本次未改工具清单,无需 `npm run build-matrix`。作者声明可信。

### 5. 仓库级约束(AGENTS.md)核查 — **通过**

**独立副本同步约束**:grep `src\tools\rule-templates.ts` 对 5 个 prompt 名零命中;grep `.claude\rules\` 对 prompt 名零文件命中。本次改动**不涉及** `.claude/rules/` 或 `rule-templates.ts`,无需双写同步。作者声明可信。

**check-rules-version-bump CI 规则**:该规则只校验 `rule-templates.ts` 变更时强制 version bump(`scripts\check-rules-version-bump.mjs`)。本次未改 rule-templates,**不触发**该规则。

**分发产物边界**:本次改动 `src\prompts.ts` 是 server 自身代码(非分发模板),`src\tscn\tscn-parser.ts` 是 server 自身 parser(非分发脚本)。均不属于 AGENTS.md「分发产物与独立副本边界」表格列出的产物/模板源。**无需特殊处理**。

### 6. 验证完整性 — **通过(间接证据)**

**`npm run build` 已验证**:`build\prompts.js` 已包含 `scene_editing_strategy` entry,`build\tscn\tscn-parser.js` 已包含 4 个新类型分支。tsc strict 模式下零错误才会有完整 build 产物。**作者声明"build 0 错误"可信**。

**`npm run lint` 已验证(范围澄清)**:`eslint.config.js:8` 的 `files: ['src/**/*.ts']` + `package.json:32` 的 `"lint": "eslint src/"` 共同确认 **ESLint 只检查 `src/`,不检查 `test/`**。本次新增的 `src\prompts.ts:85-153` 代码经审无 `any`/未用变量/prefer-const 违规。**作者声明"lint 0 警告"可信**(针对 src/)。

**`npm test` 失败项核实**:`test\e2e-resilience-headless.test.ts:25-35` 显示该测试依赖 `GODOT_PATH` 环境变量和真 Godot 进程,`if (!hasGodot)` 会跳过。该测试内容是 process-state 孤儿进程清理(`src\core\process-state.js`),与本次 4 个改动文件(prompts/tscn-parser 及其测试)**零代码关联**。失败为环境问题(GODOT_PATH 缺失或真进程行为),与本次无关。**作者声明可信**。

### 7. 潜在问题深挖 — **发现 1 个 Nit(N-1 已修复)**

**PackedInt32Array 空数组**(`src\tscn\tscn-parser.ts:183-187`):实测 `extractBalancedParenContent('PackedInt32Array()', 'PackedInt32Array')` 逻辑 —— `startsWith(prefix)` ✓、`endsWith(')')` ✓,循环从 `prefix.length-1`(即 `(`)开始,首字符 `(` 让 depth=1,末字符 `)` 让 depth=0 且 i===length-1,返回 `input.slice(prefix.length, i)` = `''`(空串,非 null)。故返回 `{__type: 'PackedInt32Array', value: ''}`,与注释 :182 一致。**正确**。

**畸形输入 `Vector2i(1, 2`**(缺右括号):`extractBalancedParenContent` 检查 `endsWith(')')` 失败 → 返回 null → 代码 `return trimmed`(返回原始字符串 'Vector2i(1, 2')。**有 fallback,不崩**。测试套件有 fuzz 测试(`test\tscn-parser.test.js:295-303`)兜底验证任意输入不抛错。

**Vector2 vs Vector2i 误匹配风险**:见维度 1,已实测确认无风险。

**prompt 正文工具名引用**:逐个 grep 核实 `godot_get_context`(✓ get-context.ts:49)、`read_scene`/`create_scene`/`add_node`/`save_scene`/`inspect_node`/`edit_node`/`remove_node`(✓ scene/helpers.ts:10-12)、`screenshot`(✓ screenshot.ts:21)、`runtime_assert`(✓ runtime-assert.ts:20)、`verify_delivery`(✓ delivery.ts:52)、`validate_scripts`(✓ validation.ts:104)。**发现 `error_analyzer` 不一致,详见 N-1(已修复)**。

---

## Blocking Issues

**无。**

---

## Nits

### N-1: prompt 正文工具名 `error_analyzer` 与实际工具名 `analyze_error` 不一致 ✅ 已修复

- **位置**: `D:\GitHub\godot-mcp-enhanced\src\prompts.ts:136`
- **置信度**: 90(实测确认;真实工具名是 `analyze_error`)
- **问题描述**: prompt 正文第 2 层"闭环验证"的"失败处理"段原写的是 `断言失败 → error_analyzer 分析 → 修复`,但项目实际工具名是 `analyze_error`(`D:\GitHub\godot-mcp-enhanced\src\tools\validation.ts:102` 的 `ACTIONS` 数组 + `:636` 的 case 分支均用 `analyze_error`)。`error_analyzer` 是**模块文件名**(`src\error-analyzer.ts`),不是工具名。
- **证据**:
  - `D:\GitHub\godot-mcp-enhanced\src\tools\validation.ts:102` `'analyze_error',`
  - `D:\GitHub\godot-mcp-enhanced\src\tools\validation.ts:636` `case 'analyze_error': {`
  - 全局 grep `error_analyzer` 仅命中本 prompt 正文(:136)+ build 产物 + plan 文件(`.zcode\plans\plan-sess_959b1472-dc31-46ab-9479-8034fce32ee4.md:123,147`)+ 一份旧设计文档图表。无任何工具定义使用此名。
- **根因**: 作者照搬了 plan 文件 `.zcode\plans\plan-sess_959b1472...md:147` 的措辞("文本里提及真实工具名...validate_scripts/error_analyzer"),plan 阶段就已笔误,作者未核对实际工具名。
- **影响**: AI 按 prompt 指引调用 `error_analyzer` 工具会得到 "Unknown tool" 错误,需自行纠错到 `analyze_error`。属 prompt 指引质量瑕疵,不影响 server 核心功能。
- **修复**: 2026-08-09 17:17 作者当场修复,`src\prompts.ts:136` 改为 `analyze_error 分析`。重跑 `npx vitest run test/prompts.test.ts` 20/20 passed。

### N-2(可选): tscn 单测未覆盖真实 fixture 文件

- **位置**: `D:\GitHub\godot-mcp-enhanced\test\tscn-parser.test.js:180-199`
- **置信度**: 60(改进项,非 bug)
- **问题描述**: 4 类型测试用 inline 字符串,未读 `test\e2e-scene\scenes\test_3d.tscn` 真实文件。若真实 .tscn 有微妙格式差异(如 `Transform3D( 1, 0, 0, ... )` 带前导空格),单测抓不到。
- **修复建议**: 可选加一个集成测试,`readFileSync(test_3d.tscn)` → `parseTscn` → 断言 Camera3D 节点的 transform 属性 `__type === 'Transform3D'`。非阻断,留 follow-up。

---

## 值得进 memory 的工程教训

**lesson: prompt 正文工具名必须 grep 实测,不能照搬 plan 措辞**

本次 `error_analyzer` 笔误的根因是 plan 阶段(`.zcode/plans/plan-sess_959b1472...md:147`)就已把工具名写错(把模块文件名 `error-analyzer.ts` 当成了工具名),实现时照搬 plan 措辞未核对。规避方法:prompt 正文里每提及一个工具名,落盘前必须 `grep "name:\s*['\"]<工具名>['\"]"` 在 `src/tools/` 下实测确认。这条适用于所有 future prompt 新增。

> 已登 memory: `lesson-prompt-tool-name-must-grep`(2026-08-09 补登)。

---

## 相关文件清单(绝对路径)

改动文件:
- `D:\GitHub\godot-mcp-enhanced\src\tscn\tscn-parser.ts`
- `D:\GitHub\godot-mcp-enhanced\src\prompts.ts`
- `D:\GitHub\godot-mcp-enhanced\test\prompts.test.ts`
- `D:\GitHub\godot-mcp-enhanced\test\tscn-parser.test.js`

实测关联文件:
- `D:\GitHub\godot-mcp-enhanced\src\GodotServer.ts`(handler 通用性)
- `D:\GitHub\godot-mcp-enhanced\src\tools\get-context.ts`(workflows 自动暴露)
- `D:\GitHub\godot-mcp-enhanced\src\tools\scene\helpers.ts`(operation_type 核实)
- `D:\GitHub\godot-mcp-enhanced\src\tools\validation.ts:102,636`(analyze_error 真实工具名)
- `D:\GitHub\godot-mcp-enhanced\test\e2e-scene\scenes\test_3d.tscn:36`(Transform3D 真实样本)
- `D:\GitHub\godot-mcp-enhanced\src\tools\rule-templates.ts` + `D:\GitHub\godot-mcp-enhanced\.claude\rules\`(独立副本边界)
- `D:\GitHub\godot-mcp-enhanced\eslint.config.js:8`(lint 范围仅 src/)
- `D:\GitHub\godot-mcp-enhanced\test\e2e-resilience-headless.test.ts:25-35`(失败项环境依赖)

---

**审查完成。判定 SHIPPED WITH NITS —— N-1 已修复,N-2 可选留 follow-up,可发版。**
