# G7 能力 profile 审查:SHIPPED WITH NITS

**日期**:2026-08-13
**审查对象**:G7 basic preset + 默认 basic(附录 E.4 战略批最后项,借鉴 GoPeak)
**审查者**:code-reviewer 子 agent(隔离视角,grep/read 实测)
**实现者**:主代理
**最终判定**:**SHIPPED WITH NITS**(N1 已修,O1 defer,无 Blocking)

---

## 实现摘要

G7 复用 enhanced 现有三层工具暴露机制(profile/manage_tools/action-gate),非新建第四层:
- `PROFILES['basic']`(= lite 9 组,对齐 GoPeak compact)
- 默认 profile `'full'` → `'basic'`(**BREAKING**,省 ~60% context,79KB→~30KB)
- `KnownProfile` 加 `'basic'`(防 fail-closed 回退 minimal)
- action-gate 默认全 gate 兜底 RCE(basic 下 execute_gdscript 可见但 gated)

## 审查通过项(8/8)

| 项 | 结论 | 证据 |
|---|---|---|
| basic preset 正确(= lite 9 组) | ✅ | `tool-registry.ts:208`;profile-basic 测试锚定 basic=lite |
| BREAKING 默认改 basic + 三条回退路径 | ✅ | `index.ts:77` 默认 basic;`--profile=full`/`GODOT_MCP_PROFILE=full`/`GODOT_MCP_MODE=full` 回退 |
| KnownProfile + fail-closed | ✅ | `ToolDispatcher.ts:49` 加 basic;basic 在 PROFILES+KnownProfile 双登记,不回退 minimal |
| action-gate 兜底 RCE | ✅ | basic 暴露 core(含 script.execute_gdscript),action-gate 默认全 gate → ACTION_GATED(调用链 trace 确认) |
| 复用三层非新层 | ✅ | profile(PROFILES/getFilteredTools)+ manage_tools(动态扩容)+ action-gate(RCE 门控),零新机制 |
| 测试覆盖 | ✅ | profile-basic(6)+ tool-groups(7 profiles) |

## Nits

### N1. README 缺 BREAKING 说明 → **已修**
plan 要求 README BREAKING 说明。原 README 无 profile 段。**修复**:README 环境变量段加 `GODOT_MCP_PROFILE` 行 + BREAKING callout(默认 full→basic + 三条回退 + manage_tools 升级 + RCE gated)。

### O1. GodotServer 兜底默认未同步 basic → **defer**
`GodotServer.ts:192` 兜底 `mode ?? 'full'`(审查建议同步 basic)。但改 basic 破 godot-server.test.js(假设默认 full,2 failed)。**决策 defer**:GodotServer 兜底保持 full(生产经 index.ts 默认 basic 真正入口,兜底是防御性边缘);O1 一致性 defer 到未来统一(若改,需同步修 godot-server.test 假设)。注释标明 defer 理由。

### O2/O3(观察,<80,记录)
- agent-context/state-store 默认 'full' drift(死字段/持久化,不影响功能)
- profile-basic 未测 index.ts 默认 toolMode(集成层,低风险,逻辑简单)

---

## 验证(修复后)

- `npm run lint`:✅ 零错误
- `npm run build`:✅ tsc 零错误
- `npm test`:✅ **5132 passed / 0 failed**(profile-basic 6 + tool-groups 7 + 零回归)
- 手动建议:启动 server(默认)→ tools/list 确认 basic 裁剪(9 组);`GODOT_MCP_PROFILE=full` → 全 41

---

## 工程教训(进 memory)

1. **BREAKING 默认值改动的测试影响面**:改 GodotServer 兜底 full→basic 破 godot-server.test(假设默认 full)。教训:改默认值(尤其兜底)要 grep 测试断言该默认;兜底默认的 O1 一致性 nit 需权衡测试影响,可 defer。
2. **复用现有机制 > 新建层**:G7 复用 enhanced 已有三层(profile/manage_tools/action-gate),零新抽象,改动最小(3 源文件 + 2 测试)。子代理建议"映射到现有三层,非新建第四层"是正确路径。
3. **BREAKING 改动的用户通知**:默认 profile full→basic 是 BREAKING,README 显著说明(回退 + 升级 + RCE 语义)是必备,N1 抓到此缺口。

---

## 相关文件
- `src/core/tool-registry.ts`(PROFILES.basic:208)
- `src/index.ts`(默认 basic:77 + 回退分支)
- `src/core/ToolDispatcher.ts`(KnownProfile:49)
- `src/GodotServer.ts`(O1 defer 注释:192)
- `README.md`(N1 BREAKING:环境变量段)
- `docs/THREAT_MODEL.md`(§5 basic)
- `test/core/profile-basic.test.ts`(6 测试)
- `test/tool-groups.test.js`(7 profiles)
