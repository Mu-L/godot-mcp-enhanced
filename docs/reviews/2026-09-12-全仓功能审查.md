# 全仓功能模块漏洞与业务逻辑审查报告

> **日期**:2026-09-12
> **范围**:v0.32.21 全部功能模块(P0-P10 迭代后)——46 工具/271 action 的漏洞、业务逻辑正确性
> **方法**:4 个分域只读审查代理( dap / mcp_bridge.gd / TS 执行安全链 / bridge TS+生命周期)逐行实测 + 主审横向机械扫描(TODO/空 catch/定时器/动态正则/spawn 注入面) + 业务冒烟(smoke 72 用例/CLI doctor)
> **处置**:BLOCKING 与 IMPORTANT 直接修复并补回归测试;架构级/低影响备案

---

## 一、发现总览

| 域 | BLOCKING | IMPORTANT | MINOR | 已修 | 备案 |
|---|---|---|---|---|---|
| TS 执行安全链 | 1 | 1(+1 降级设计声明) | 4 | 6 | 2 |
| dap 工具 | 0 | 4 | 3 | 6 | 1(M-7 报错误导) |
| bridge TS + 生命周期 | 0 | 2 | 3 | 2 | 3 |
| mcp_bridge.gd(GD 侧) | 1 | 7 | 3 | 8 | 3 |
| 横向/流程 | 0 | 0 | 0 | 0 | 2(验证盲区) |
| **合计** | **2** | **14** | **13** | **22** | **11** |

> 审查期插曲(工程教训):S-I1 初版修复(scrub 固定常量)曾致全量 41 用例失败——二分定位(revert 单项对照)+取证 log 破案:测试模板**有意**使用固定常量依赖 C-09 替换机制,scrub 破坏公开契约。处置:撤销修复、降级发现为设计声明。教训:改"看似防御"的安全逻辑前先 grep 全仓对该机制的依赖面(契约 vs 漏洞的判定依据)。

---

## 二、已修复(23 项,按域)

### TS 执行安全链(7 项)

| # | 严重 | 问题 | 修复 |
|---|---|---|---|
| S-B1 | BLOCKING | **project_replace 写 .gd 完全绕过沙箱扫描**——`script.ts` 批量落盘只过 pluginSelfPathGuard,违反自家 `script.ts:79`"全仓所有写 .gd 落盘前必须过此扫描"约束;攻击路径与 SEC-P1-1 同构(replace 注入 OS.execute → run_project 即执行) | 批量原子检查块对每个 .gd 内容过 `scanScriptSandboxOrThrow`,命中整批拒;回归测试锁定(注入用例 6 passed) |
| S-I1 | ~~IMPORTANT~~ → **设计声明** | **随机输出标记经 replaceAll 暴露给用户代码**(审查初判)——实测**撤销修复**:固定常量 `___MCP_RESULT___`/`___MCP_ERROR___` 是 C-09 的**公开协议契约**(ui-layout 等 full-class 测试模板与生成器统一"写常量、server replaceAll 换随机 marker",scrub 破坏契约致 41 用例结构化输出解析全断,二分定位+取证 log 破案)。威胁模型重审:单脚本内输出控制权本属脚本作者(marker 防跨执行/重放伪造非当次伪造);execute 通道注入防御由 untrusted 信封(S-M3)承担 | 修复撤销,保留处置记录注释(gdscript-executor.ts executeGdscript 入口);审查发现降级为设计声明 |
| S-I2 | IMPORTANT | **cmdline 标记窃取链**——`OS.get_cmdline_args()` 取回脚本路径 + 变量首参 `FileAccess.open` 读包装后脚本正文恢复随机 marker(两条 FileAccess 模式均不匹配变量首参,黑名单盲区) | DANGEROUS_PATTERNS 增补 `OS.(get_cmdline_args\|cmdline_user_args)` |
| S-M1 | MINOR | cleanupOldSessions 正则 `/-(\d+)-$/` 与 mkdtemp 随机后缀永不匹配(A-02 文件名时间戳解析从未生效,恒走 mtime 兜底) | 改前缀锚定 `^TMP_PREFIX(\d+)-`(引用常量防漂移) |
| S-M3 | MINOR | execute_gdscript 的 outputs/raw_output 无信封包装(项目文件内容经 _mcp_output 回传是 P1-1 威胁模型间接读通道,workflow 通道有包 execute 通道裸吐) | outputs.value 与 raw_output 过 `maybeWrapUntrusted`(对齐 workflow.ts) |
| S-M4 | MINOR | wrapUntrusted 的 kind/source 属性未转义(source 来自用户 _mcp_output key,含 " 逃逸信封开标签属性域) | `"` → `&quot;` 转义 |
| — | — | dap.ts 两处 `if (Boolean(...))` lint 遗留(P9 N-1 清偿漏跑 lint) | 改直接 truthy 判断 |

### dap 工具(6 项)

| # | 严重 | 问题 | 修复 |
|---|---|---|---|
| D-I1 | IMPORTANT | **output 收集 200 条稳态后永久静默失效**——`_collectOutput` 用 `messages.length` 做游标,与 `_trimMessages` 的 200 上限**数学互斥**(稳态下 push 即 shift,length 恒 ≤200,`slice(200)` 恒空数组);游戏 print 刷屏超 200 条后 output action 恒返回空 | 消息附单调 `__mcpSeq`(trim 不影响),按 seq 过滤窗口新消息;所有输出出口剥离内部字段(探针暴露的 response 污染一并修);回归测试 DAP-s(205 条稳态后窗口内 3 条新 output 必须可见) |
| D-I2 | IMPORTANT | **断连重建状态机绕过 + 断点簿记静默清空**——socket FIN 断开(只触发 close 不触发 error)后残留 session initialized=true,后续带前置的 action 经旧 session 校验通过但 `_ensureSession` 重建未握手新连接(协议违规+误导性超时),同时 `_closeSession` 静默清空断点簿记 | EnsureSessionResult 加 `rebuilt` 标记,重建后带前置的调用立即返回"re-run initialize" |
| D-I3 | IMPORTANT | **断点簿记 key 无路径规范化**——`D:/a.gd` 与 `D:\a.gd`(及大小写/./..变体)生成多份独立簿记,与 editor 实态漂移(幽灵断点/上限绕过/list 虚高) | `_normalizeSourceKey`:resolve + Windows 小写 + 正斜杠统一(res:// 原样) |
| D-I4 | IMPORTANT | **DAP-o 测试虚假覆盖**——"超时路径带 messages"用例实际命中 endpoint 守卫错误(注释自称"先 initialize 建会话"但代码没做),messages 断言整体缺失 | 重写:mock2 先 initialize 建独立会话再 threads 走真超时,显式断言 `error_type=dap_timeout` + messages 非空;连带发现 close 悬挂(成功会话连接不断开则 server.close 永悬)修为 finally 先 disconnect |
| D-M5 | MINOR | capabilities 是全文件唯一未过 `_sanitizeValue` 的 DAP 数据出口(远程恶意 server 内嵌 token 裸吐) | 统一过清洗 |
| D-M6 | MINOR | buffer 上限=frame 上限=1MB,恰好 1MB 的合法帧先被 buffer 上限误杀(报错误导) | buffer 上限 +4KB header 余量 |

### bridge TS + 生命周期(2 项)

| # | 严重 | 问题 | 修复 |
|---|---|---|---|
| B-I1 | IMPORTANT | **sync_state 丢弃 GD 侧 truncated 截断标志**——P10 审查 N-2 在 GD 侧产出 truncated=true(N-2 修复声明"防两侧同截断假阴性"),但 TS 消费侧(SyncSnapshot 接口/存储/compare)完全丢弃:256+ 节点场景同截断→in_sync 假阳性、不同截断→missing 假阴性,**N-2 只修了一半** | SyncSnapshot 加 truncated 字段;snapshot 透传+警告;compare 标注 truncated_a/b + `unreliable: true`;list 透传;契约测试 TS-b3 锁定 |
| B-I2 | IMPORTANT | **game_wait 单次 probe 超时烧掉整个等待预算**——probe 超时=interval×2(默认 400ms),游戏主线程卡顿>400ms(场景加载/物理重帧常见)即 BridgeTimeoutError 上传播立即失败,30s 总预算只消耗 0.4s;错误建议"加大 timeout 重试"无效(不改变 probe 超时)——**与同日 p7-e2e 的 400ms 假失败同根,当时只修了测试参数,本次修产品根因** | pollWaitCondition 对 probe throw 容错(视为本轮不可用继续轮询,预算耗尽走正常 timed_out);响应内 error 字段仍立即中止(既有语义不放松);回归测试 2 用例(先 reject 后恢复满足/持续 reject 到预算尽) |

### mcp_bridge.gd(8 项)+ websocket_server.gd(1 项)

| # | 严重 | 问题 | 修复 |
|---|---|---|---|
| G-B1 | BLOCKING | **step_until 完成判定嵌在 for cond 循环体内**——① AND 语义破坏:conditions=[A,B] 时 A 满足即完成,B 未评估,predicate_met 谎报全满足(qa runner/工具 schema 都把多条件当一等用法) ② 同 su_idx 双 append(帧耗尽/多条件场景每条件命中一次)→ 消费循环倒序双 remove:单 pending 时空数组越界**中断 _process 当帧**(monitor/watch/custom tick 全跳过),多 pending 时**误删相邻条目**(响应永久丢失+错误 refreeze) | 判定移出循环:全部条件评估后统一判定,每 entry 每帧至多一次 append;多条件 AND 语义 e2e 回归锁定(A 满足 B 拒斥 → predicate_met=false + 帧耗尽 30) |
| G-I1 | IMPORTANT | step_until 校验失败仍抢注 owner + 残留过期 paused 原值(注释声明"校验必须在开窗副作用之前"但 owner 赋值/paused 保存仍在校验前)——校验失败即抢锁拒其他 peer;peer 断线还原到过期 paused(游戏自身后开的暂停被重置) | owner 赋值与 paused 保存随开窗副作用一并移到全部校验之后 |
| G-I2 | IMPORTANT | find_nodes 的 near_node 锚点缺 player 档可观察性检查——private/级联隐藏节点可当锚点拿到与各可观察节点的 distance,每帧查询差分追踪 private 位置轨迹(穿透 P7 存在性不泄露承诺,且与 position 规则检查的防差分动机自相矛盾) | 锚点补 `_observable_in_player` 检查,报 not found 语义(不泄露存在但不可见) |
| G-I3 | IMPORTANT | collect_state 树遍历无 freed 守卫——_mcp_state() 是 P10 唯一用户代码执行点,其内部 .free()(非 queue_free)树节点致后续 pop 拿 freed 引用报错中断整次收集(result=null,**已收集的 200+ 节点状态静默全丢**,AI 误判空状态) | pop 后 is_instance_valid 守卫跳过 freed 节点,收集继续(_mcp_state 自身崩溃路径 GDScript 无语言级捕获手段,如实记录) |
| G-I4 | IMPORTANT | 未认证 peer 空行保活 DoS——任何入站数据无条件刷新 idle 计时 + 空行直接 continue 不进未认证分支:每秒发 "\n" 即无限续命,5 连接占满 MAX_PEERS 永不超时,合法客户端永久被拒(打破"槽位占用均有界"声明);本地进程即可,无需 secret | 未认证空行直接断连(已认证 peer 空行容忍,keepalive 惯例);慢速散字节由 1MB buffer 上限兜底 |
| G-I5 | IMPORTANT | websocket_server 顶层标量 JSON 崩溃——"123"/1.5/"x" 经 parse_string 返回 int/float/String(无 has() 方法),`not parsed.has("jsonrpc")` 运行时错误中断 _handle_message → _process 当帧中断(所有 peer 的 poll/heartbeat 跳过);**无需认证**持续发送即实质瘫痪 editor WS 服务 | 改 `not (parsed is Dictionary)`(对齐 bridge 侧既有正确写法) |
| G-I6 | IMPORTANT | custom 命令协程 quiesce 失真——命令含 await 时 `callable.call()` 在首个 await 挂起立即返回,active_calls 立即归零(quiesce 不覆盖仍在执行的协程,下个 tick 热重载释放其宿主实例),且返回值流入响应变无意义字符串 | get_class() 字符串比较检测协程(注意:`is GDScriptFunctionState` 类型检查在 4.x 会 Parse Error 挂掉 autoload——实测踩坑)→ push_warning + 返回结构化 error |
| G-M2 | MINOR | playtest.step 无 owner 互斥(seed/fixed_delta/snapshot/restore 都有)——owner A 确定性重放期间其他 peer 的 step 可推进帧破坏帧对齐 | 补 `_playtest_owner_pid` 检查(playtest 域,对齐 seed 同款)+调用点传 pid |

---

## 三、备案未修(10 项,理由)

| # | 项 | 理由 |
|---|---|---|
| N1 | GD I-7:响应走阻塞 put_data,慢消费者可冻结游戏主线程 | 架构级改造(出站队列+partial drain),风险大收益窄(secret 前提下意外挂起场景罕见);下批专项 |
| N2 | GD M-1:`_observable_in_player` 级联 64 层截断 fail-open | 64 层深树极罕见;fail-closed 改造涉语义权衡 |
| N3 | GD M-3:playtest.snapshot 递归遍历只限总量不限深度 | 50000 层深链先爆栈场景理论性;与 get_scene_stats 迭代式对齐属一致性重构 |
| N4 | S-M2:file-guard 覆盖面与 AGENTS 红线清单不符(server 侧不拦 build/node_modules/锁文件) | 设计声明如此("只拒插件自资产");文档化即可,拦截范围扩展另议 |
| N5 | D-M7:set_settings 无法单独关 allow_remote_hosts(报错指向未修改的 host) | 安全方向过严无害;同批次传 host 可解 |
| N6 | bridge MINOR:`_syncSnapshots` 无上限增长 | 有 clear 子操作兜底+有意设计(进程生命周期);agent 循环自增 label 属误用 |
| N7 | bridge MINOR:valuesEqual 注释与 NaN 行为表述不符 | 行为本身更安全;纯注释瑕疵 |
| N8 | bridge MINOR:process-state 的 _projectDir/_outputBuffer 在 close() 不清 | AGENTS 已声明的存量 23 setter 清算长线项 |
| N9 | helpers.ts barrel "TODO v0.20.0 remove" 逾期 12 个 minor | 历史欠账非本轮范围 |
| N10 | dap 会话无空闲超时(生命周期靠显式 disconnect/上限逐出) | 设计如此(单请求超时 30s 有);非缺陷 |

---

## 四、流程与验证盲区(新发现,2 项)

1. **check:gdscript 对 autoload parse error 漏报**:本次 `GDScriptFunctionState` 类型检查 Parse Error(autoload 直接挂掉)在 `npm run check:gdscript` 下 errors=0(手起游戏才暴露)。工具覆盖 src/scripts 但其 import 模式的报错口径不含该类错误——需另开小项调查。
2. **e2e fixture 的 mcp_bridge.gd drift 保守策略放大测试陈旧**:install 对已存在副本"内容 drift 保旧文件"——gd-symmetry 用旧版 bridge 跑测试时 GD-B1 修复"看似无效",实为 fixture 副本陈旧(gitignore 的运行时产物)。e2e 起游戏前应保证副本新鲜(或 install 对 e2e fixture 强制覆盖)。

另:同日早前 p7-e2e/gd-symmetry 的 400ms/wall 假失败,本次经 B-I2 修掉产品根因(pollWaitCondition probe 容错)后,测试侧参数加固(interval_ms/wall_budget)成为纵深防御双保险。

---

## 五、已检查无问题的维度(四代理交叉确认,带证据见各代理报告)

帧协议粘包/半包/Content-Length 注入、loopback 强制(N-2 白名单式过严无绕过)、_connectInflight 并发去重、pendingError 通道闭环、_sanitizeValue 嵌套覆盖、路径白名单双层 realpath/junction/UNC/8.3、nonce 防重放(HMAC 常时比较/双条件清理)、进程树清理(Windows taskkill /T + POSIX pkill -P)、Symbol 信任传递、Scanner 大小写/空白/Unicode/动态构造/preload 链、P8 热加载状态机(debounce/独立加载/回滚/重名冲突)、哨兵通道伪造面(顶层键全部硬编码)、_ErrorCapture 递归防护、freeze/paused 单槽还原(D-2)、input_seq 无双 append、keepalive 断线不循环、GodotServer.close() 19/24 setter 配对清理+定时器全覆盖。

---

## 六、验证证据

- `npm run lint` 退出码 0;`npm run build` 过;`npm run check:gdscript` errors=0
- 分域复验:dap.test **20/20**(含重写 DAP-o/新增 DAP-s)、script-project-replace **6/6**(含 B-1 注入拒绝)、game-bridge-wait **8/8**(含 2 个 probe 容错)、p10-unit **15/15**(含 TS-b3)、gd-symmetry L2 **5/5**(含 GD-B1 多条件 AND)、g1 契约 **22/22**
- 手起 fixture 实证 GD 修复前后对比(GDScriptFunctionState parse error → 修正后正常启动)
- 全量 `npm test` 与 6 文件 L2 串行复验:见本报告提交时输出
