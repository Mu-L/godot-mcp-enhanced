# 第三方审查:C-安全批次 SEC-P2-1 + SEC-P2-2

**审查日期**:2026-08-09
**审查对象**:`feat/sec-batch-p2-1-p2-2` 分支工作树(审查时未 commit)
**审查者**:code-reviewer 子 agent(隔离视角,所有声明 grep/read 实测复核)
**被审查批次**:38 open 待办的 C-安全批次(第 2 批,A 类清状态已完成)

## 总体判定:**SHIPPED WITH NITS**(0 Blocking + 2 Nit,Nit-1 已当场修复)

核心安全加固逻辑正确、对称、有真实测试覆盖(非假绿)、仓库级约束全部满足。

## 批次构成与基线核实(关键:3 项状态已变,非照搬待办原文)

| # | 待办 | 待办描述 | 核实结果(grep/read) | 处理 |
|---|------|---------|---------------------|------|
| 1 | profile 硬隔离 | "当前只在 :183 广告层" | ✅ 已落地 `ToolDispatcher.ts:258-265` executeToolCall 入口 isToolAllowed(Task 3 A-RCE #3) | 回标 |
| 2 | godot_path 白名单 | "注释提及未实现" | ✅ 已实现 `godot-finder.ts:77-93` isGodotPathAllowed,覆盖全 5 注入点 + 测试 L421-456 | 回标 |
| 3 | SEC-P2-1 test-framework | 裸 validatePath | 确认 `:85` 是裸 validatePath(仅 resolve) | 实现 |
| 4 | SEC-P2-2 editor secret symlink | GD 侧 PowerShell 不防 symlink | 确认 + 发现 bridge 侧 `mcp_bridge.gd` 对称 DUPLICATE 同样缺陷 | 实现(双侧) |
| 5 | SEC-P2-6 重复实现 | "语义一致需手动同步" | ❌ 待办描述不准——gdEscape 转义 %%/',serializeGdValue 不,语义有差异 | DEFERRED |
| 6 | verifyApiToken 接线 | "零生产调用" | 确认 + 属架构债(需加 HTTP server) | DEFERRED |

**关键教训**:6 项里 3 项待办描述已过时(profile/godot_path 已落地、SEC-P2-6 语义描述不准)。若不基线核实直接实现,会重复劳动(profile/godot_path)或改变行为(SEC-P2-6)。

## SEC-P2-1 审查结论

| 要点 | 结论 | 证据 |
|------|------|------|
| requireProjectPath 含 isPathInAllowedRoots(deny-by-default) | ✅ | `helpers.ts:110-116` → `path-utils.ts:258-283`(allowed.length===0 时 deny-by-default) |
| validatePath 在 test-framework.ts 代码零引用 | ✅ | grep 仅注释命中 |
| 删 typeof 后 project_path 缺失仍妥善处理 | ✅ | requireString 抛错 → catch `:95-97` → opsErrorResult('INVALID_PATH') |
| opsErrorResult import 未 unused | ✅ | 仍被 9 处使用 |
| 错误码 INVALID_PARAMS → INVALID_PATH 语义合理 | ✅ | catch 统一包装路径类错误 |

## SEC-P2-2 审查结论

| 要点 | 结论 | 证据/推理 |
|------|------|----------|
| PowerShell 注入面无新增 | ✅ | `$env:_MCP_SECRET_PATH` 经 env 传递(F-1),新增关键词都是固定字面量 |
| PowerShell 语义正确 | ✅ | Test-Path 不存在返 $false 不抛错守 Get-Item;-LiteralPath 防通配符;.LinkType 对 symlink 返 "SymbolicLink";`exit 3` 在 -Command 模式立即终止进程设 exit code=3,WriteAllText 不执行 |
| readlink 跨平台语义 | ✅ | Linux/macOS readlink 普通文件返非零、symlink 返零;GD OS.execute 返进程 exit code |
| 两处对称性 | ✅ | editor void 方法 `_secret=""+return`,bridge bool 方法 `return false`,签名不同致语义不同,各自正确 |
| 两处都不 fallback FileAccess | ✅ | symlink 命中早退。PowerShell 非 symlink 故障的 fallback follow 窗口是可接受取舍(见 Nit-2) |
| DUPLICATE 互引 | ✅ | websocket_server.gd:89 + mcp_bridge.gd:385 互提对方 |
| check:gdscript 是项目级完整编译 | ✅ | `check-gdscript.ts:103` 跑 godot --headless --import(非逐文件 parse) |

## 测试质量

| 要点 | 结论 | 证据 |
|------|------|------|
| reject 测试非假绿 | ✅ | setup.js:6 全局 UNRESTRICTED=true;beforeEach vi.stubEnv('') 真实覆盖(Vitest stubEnv 直接改 process.env);reject 用 /outside/allowlist 不匹配 /allowed/root → 真拒绝 |
| stripComments(非 stripCommentsAndStrings)取舍 | ✅ | 关键词本在 PowerShell 命令字符串内(会被执行),strip 字符串会误删真关键词;只 strip 注释即可防假绿 |
| 契约测试覆盖度 | ✅ | 7 it 覆盖双侧 Windows/Linux 预检 + 拒写语义 + DUPLICATE 互引 + 等长校验 |

## 仓库级约束核查(AGENTS.md 强制)

| 约束 | 结论 |
|------|------|
| build/scripts/mcp_bridge.gd 同步 | ✅ 含 Test-Path/exit 3/readlink |
| 无第三副本遗漏(生产路径) | ✅ addons + src/scripts + build 三处同步 |
| test/fixtures/gdscript-check | ✅ 运行时重拷非手维护 |
| test/fixtures/real-project(手动快照) | ⚠️ Nit-1(已当场修复,见下) |

## Blocking Issues

**无。**

## Nits

### Nit-1: real-project fixture 第三副本漂移(已当场修复)
- **位置**:`test/fixtures/real-project/addons/godot_mcp_server/websocket_server.gd`
- **问题**:手动快照未同步 symlink 预检(grep 0 命中),还缺 CMP-14 debug 改动
- **影响**:不影响生产(生产用 addons/ 主副本);fixture 仅 E2E 本地测试(CI skip)
- **修复**:当场 `cp addons/.../websocket_server.gd test/fixtures/real-project/addons/.../`(单文件精准同步),grep 复核 5 关键词命中,check:gdscript 仍 0/0

### Nit-2: Windows fallback FileAccess 的 symlink follow 窗口(设计取舍,保留现状)
- **位置**:websocket_server.gd:129-137 + mcp_bridge.gd:422-429
- **观察**:symlink 命中(ec==3)已正确早退。但 PowerShell 非 symlink 故障时 fallback FileAccess 会 follow symlink
- **评估**:可接受取舍——PowerShell 系统自带几乎总可用 + 双重前提概率极低 + 读方 lstatSync 兜底 + 注释意图清晰
- **处理**:保留现状(满足 SEC-P2-2 声明目标)

## 值得进 memory 的工程教训(已登记)

1. **stripComments vs stripCommentsAndStrings 取舍**:关键词在字符串字面量内(命令串)时只 strip 注释
2. **vi.stubEnv 覆盖 setup.js 全局**:验证拒绝路径须独立 describe beforeEach 显式清空 + afterEach 恢复
3. **GD OS.execute = 子进程 exit code**:PowerShell -Command 模式 exit N 立即终止;readlink 普通文件非零/symlink 零
4. **secret 写 symlink 对称设计**:读方 lstatSync 兜底 + 写方预检,DUPLICATE 互引,fixture 手动快照易漂移

## 验证清单(实跑输出)

| 命令 | 结果 |
|------|------|
| `npm run lint` | 0 error |
| `npm run build` | tsc 0 error + .gd 拷贝成功 |
| `npm test` | 329 files passed / 4834 tests passed / 27 skipped(零回归) |
| `npm run check:gdscript` | errors=0 warnings=0(项目级完整编译) |
| `grep -c "^- \[ \]" 项目待办.md` | 38 → 30(A 类 -4 + C 类回标 4) |

## 关联

- 开发日志:`Obsidian/GodotMCP/开发日志/2026-08-09 C-安全批次.md`
- memory:`feature-decision-2026-08-09-sec-batch-p2-1-p2-2` + `engineering-lesson-stripcomments-vs-stripstrings-gd-contract`
- 待办回标:`项目待办.md` :268(profile)/ :270(godot_path)/ :703(SEC-P2-1)/ :704(SEC-P2-2);DEFERRED 加注 :225(verifyApiToken)/ :705(SEC-P2-6)
