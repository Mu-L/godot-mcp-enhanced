# 第三方审查:docs/tools/ 文档自动生成(项目待办 :154)

**审查日期**:2026-08-10
**审查对象**:gen-tool-docs.mjs + docs/tools/*.md(40)+ 测试 + package.json
**审查者**:code-reviewer 子 agent(grep/read 实测)

## 总体判定:**SHIPPED WITH NITS**(0 Blocking + 2 Important 已修 + 3 Nit)

脚本质量高(纯函数分离/幂等/防御性清理/字段映射正确),40 文件生成正确,help 工具集成验证通过。审查发现 2 个 Important(npm 分发盲区 + help 零测试),均已当场修复。

## 改动摘要

从 `docs/capability-matrix.json`(权威数据源)自动生成 `docs/tools/{name}.md`(40 个),作为 help 工具(help.ts:74-86)运行时读取的数据源。新增 `scripts/gen-tool-docs.mjs`(3 导出纯函数)+ `npm run gen:tool-docs` + 23 个新测试(17 生成脚本 + 6 help 端到端)。

## 逐维度核实

### 1. 生成脚本 — PASS
- `inferLayer` 三层判定正确(editor/bridge/headless),bridge 特判 name=runtime/runtime_assert 覆盖完整
- `extractParamRows` action 排首位、required 标 ✓、enum 简写(>3项/action 只标"enum (N项)")逻辑正确
- 幂等:全程无 Date/random,同输入同输出(测试断言 doc1===doc2)
- 字段映射与 matrix(capability-matrix.json:76-89 readonly/longRunning/securityLevel/riskDistribution/needsGodot/needsEditor)一致

### 2. 生成质量 — PASS(抽查 8 个)
scene.md(19 actions/31 参数)、editor.md、game.md(bridge 层正确)、runtime.md 等,内容与 matrix 一致,页脚 AUTO-GENERATED 全 40 文件命中。

### 3. 覆盖完整性 — PASS
matrix 40 tools → 40 .md(1:1);help.ts TOOL_NAMES 38 全有对应 .md;matrix 比 TOOL_NAMES 多 debug/engine 2 个(editor-only,help enum 不含故 agent 不会请求,无害)。

### 4. help 工具集成 — PASS(修复后)
- 三候选路径(help.ts:74-80)在 docs/tools/ 存在后生效,开发场景候选 1 命中
- 兜底拼写纠错(:93-99)对不存在工具名降级
- **补了 6 个端到端测试**(test/tools/help.test.ts)守护此集成

### 5. 测试 — PASS(修复后)
23 个新测试:gen-tool-docs 17(纯函数 + 幂等 + 覆盖)+ help 6(端到端 + 拼写纠错 + 路径遍历)。

### 6. 仓库级约束 — PASS
- 不挂 prebuild(避免 git diff 噪音)决策合理
- docs/tools/ 进 git(.gitignore 不含 docs)
- matrix 不存在时脚本防御性报错退出

## Important Issues(均当场修复)

### Important-1:npm 包不含 docs/tools/,help 在 npm 安装后失效(已修复)
**问题**:package.json `files` 字段不含 docs,npm publish 后包无 docs/tools/,help 三候选路径全失败降级为拼写纠错。README 同时引导 `npx`(npm 分发)和 git clone,故 npm 是真实分发渠道。
**修复**:`files` 加 `"docs/tools/**/*.md"`(只打包生成的工具文档,不打包整个 docs/ 避免臃肿)。

### Important-2:help 工具零端到端测试(已修复)
**问题**:grep 全 test/ 目录,help 工具 handler 零测试。功能核心价值(help 读 docs/tools/ 返回文档)无自动化守护。
**修复**:新建 `test/tools/help.test.ts` 6 个测试(读 android.md / 拼写纠错 / Did you mean / tool_name 缺失 / 路径遍历 basename 防护 / 非 help 工具返 null)。

## Nits(留 follow-up)
- **Nit-1**:gen:tool-docs 未链式依赖 build-matrix(有防御性报错,体验优化)
- **Nit-2**:测试 TOOL_NAMES 硬编码(注释标注同步,可接受)
- **Nit-3**:缺空字段边界测试(脚本有兜底,补强项)

## memory 教训
1. **生成产物分发链路审查盲区**:`files` 字段决定 npm 包内容,易被"本地跑通"掩盖。生成 docs/tools/ 进 git 只解决 git clone 用户,npm 用户读不到。审查生成类功能必查分发链路。
2. **"作者声称测过"vs 测试代码**:对"我测过了"声明,必须 grep 测试目录验证有无可复现测试,口头声明不计数。

## 验证
- lint 0 / build 0 / test **4886 passed**(+23 新:17 生成脚本 + 6 help) / 生成 40 文件 / help 端到端读 android.md / 幂等 0 diff
