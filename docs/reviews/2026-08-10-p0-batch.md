# 第三方审查：P0 四项修复（2026-08-10）

> 审查者：code-reviewer 子 agent（隔离视角，所有声明 grep/read 实测）
> 审查对象：分支 `feat/multi-instance-receiver-and-e2e-asset-harness` 2026-08-10 的 P0 四项修复（项目待办 :401/:402/:403/:404）
> HEAD：db09c75（审查时工作区有未提交改动）

## 总体判定

**SHIPPED WITH NITS** — 核心改动设计正确、测试充分、部署同步完整、CI 改动合理。无 Blocking Issues。3 个 Nit 均不阻断合并。

实现者关于"4 个 P0 基于过时快照、实际工作量远小于描述"的声称**逐项 grep/read 实测后基本证实属实**。

**审查者工具受限说明**：code-reviewer 子 agent 无 Bash 工具，无法独立复跑 `npm run lint/build/test`。F 段做了全部能做的静态验证（类型链路、引用完整性、eslint 规则对照、文件存在性），但"4911 测试全绿"等运行时声明无法独立证实。合入前人工实跑 `npm run lint && npm run build && npm test` 做最终把关（实现者已贴输出：lint 0 / build 0 / test 4911 passed / check:tool-count 40/228 全绿）。

---

## 逐维度结论

### A. 设计正确性 — 通过

**A.1 animtree TOOL_META** — 证实（`src/tools/animtree.ts:447-465`）
5 action 标 write + play 标 read，与声称完全一致。GD 侧 `addons/godot_mcp_server/commands/animtree_commands.gd` 实测确认：
- `animtree_create`（:114 `parent_node.add_child(tree)` + undo :102-112）
- `animtree_add_state`（:156 `sm.add_node(...)` + undo :151-154）
- `animtree_add_transition`（:209 `sm.add_transition(...)` + undo :204-207）
- `animtree_set_blend`（:254 `tree.set(param_name, new_val)` + undo :248-252）
- `animtree_play`（:278 `playback.travel(state_name)`）— 运行时触发，read 合理

语义分级合理：animtree 用 write（create/add 是"加"，对齐 scene add_node=write）；animation-track 用 destructive（remove 是"删"，对齐 animation delete/remove_track=destructive）。

**A.2 risk-coverage 不变量** — 通过（`test/risk-coverage.test.ts:18,57-76`）
GUARDED_KEYS 含 'animtree'，不变量跳过 animtree（:63 `continue`），不会误报。不变量本身健康（白名单机制，非放宽）。

**A.3 确认门端到端** — 证实完整（`src/guard.ts:63-68`）
`requiresConfirmation` 逻辑 `risk !== undefined && risk !== 'read'` → write 返 true 触发确认。链路无断点。

### B. 测试质量 — 通过

**B.4 risk-declarations 断言** — 精确（`test/risk-declarations.test.ts:45-68`）
animtree + animation_track describe 用 `as const` + `toBe(risk)` 逐 action 锁死值，6 case 全覆盖。

**B.5 guard.test.js 非 GUARDED 表** — 证实正确（`test/guard.test.js:132-163`）
animtree 5 写操作已移除，只留 animtree_play。反向 it（:157-163）断言 5 write→true + play→false，能捕获"改回 read"回归。

**B.6 :401 覆盖声称** — 证实"两层覆盖早已存在"
- 单元层 `test/editor-connection.test.js:305-353`：maxReconnectAttempts=3 + 真实 ws 耗尽 + exhaustedCalls 去重断言
- 集成层 `test/editor-fallback-integration.test.js:116-145`：path A 覆盖真 ws 耗尽→connectionMode='headless' + editorConn=null

**B.7 :403 假绿声称** — 完全证实
material-ops.test.js:178-179 + signal-ops.test.js:94,98 已是 toContain 完整语义断言。全 test/ grep `includes('null')`/`includes('true')` 零匹配。

### C. 部署同步 — 通过

**C.8 capability-matrix** — 证实重建（`docs/capability-matrix.json:694-703`）
animtree `riskDistribution: {read:1, write:5}` 与 TOOL_META 一致。`.md` 文件 :30 已列入。

**C.9 独立副本同步** — 不涉及
本次未触及 `.claude/rules/` 或 `rule-templates.ts`（grep 零匹配），约束不适用。

**C.10 version bump** — 不涉及
`check-rules-version-bump.mjs` 只检查 rule-templates.ts + claudemd-builder.ts，本次未改，无需 bump。

### D. defects.ts 守护 — 通过

**D.11 新 detect** — 与 animation-track 模式一致（`test/regression/defects.ts:1118-1123`）
`animtree-write-confirmation` detect 用 `readSrc('src/tools/animtree.ts').includes("animtree_create: 'write'")` 字面量匹配，能捕获"改回 read"回归。

**D.12 FIXED_DEFECTS 计数** — 证实（`test/regression/defects-fixed.test.ts:157-158`）
计数 134，grep defects.ts `status: 'fixed'` 实测 134 条，一致。

### E. CI 改动 — 通过

**E.13 ci.yml 顺序** — 证实正确（`.github/workflows/ci.yml:85-102`）
Install Godot（:85-97）在 vitest（:98-102）前。vitest 补了 `env: GODOT_PATH`（:101-102）。

**E.14 副作用** — 无负面影响
check:gdscript 仍在 Godot 之后，score:gate 不依赖测试顺序。代价 +30-60s 下载（注释已说明），timeout 10 分钟有余量。

### F. 验证完整性 — 部分受限

审查者执行的静态验证：类型链路核对 / 引用完整性 / eslint 规则对照 / capability-matrix drift / 文件存在性，均通过。

审查者无法执行（无 Bash）：`npm run lint/build/test` 实跑。建议合入前人工实跑。静态分析未发现会导致编译/测试失败的迹象。

### G. 遗漏检查 — 1 个一致性观察

**G.17 capability-matrix 重建** — 证实真跑了
riskDistribution 值与 TOOL_META 精确匹配，非仅时间戳更新。

**G.18 animation-track vs animation-ops 的 add_track 不一致** — 历史问题，非本次引入
`animation-track.ts:374` add_track 标 read，`animation-ops.ts:689` 标 write。是 2026-07-24 批次 E 的历史决策。建议后续 P1 统一，本次不阻断。

**G.19 :401 defects 守护** — 确认无独立 detect，靠测试守护（可接受）
实现者声称"defects.ts:1118-1123 同时服务 :401 和 :402"**不准确**——该 detect 名为 animtree-write-confirmation，只服务 :402。:401 靠两层测试守护（分散逻辑难用单字面量 detect 捕获，测试守护是更合适形式）。

---

## Blocking Issues

无。

## Nits（建议但不阻断）

1. **措辞修正（G.19）**：实现者交付说明里"defects.ts:1118-1123 同时服务 :401 和 :402"有误，该 detect 只服务 :402。建议修正措辞，避免后续维护者误以为 :401 有 defects.ts 守护而放松测试维护。

2. **animation-track vs animation-ops 的 add_track/add_keyframe 标注不一致（G.18）**：历史决策（2026-07-24 批次 E），非本次引入。建议后续 P1 统一"加轨道/加关键帧"的 risk 语义。

3. **animtree.ts:397 变量遮蔽**：`case 'animtree_state_edit'` 块内 `const action` 遮蔽外层 :308。语法合法、逻辑无害，但是代码异味。非本次引入，建议后续清理时改名。

---

## 值得进 memory 的工程教训

1. **P0 描述时效性核查**：本次 4 个 P0 实现者声称"全部基于 2026-07-29 过时快照"，审查逐项实测后证实属实。教训：待办 P0 描述若带具体日期快照，实现前应先 grep 当前状态确认是否已被前序批次顺带修掉，避免重复劳动。审查者也应以"实测当前状态"为准，不预设描述为真。

2. **defects.ts detect vs 测试守护的适用边界**：:401（重连耗尽）逻辑分散在 EditorConnection 多处，无法用单字面量 detect 捕获，改用两层真实 ws 测试守护（单元 exhaustedCalls + 集成 connectionMode 降级）是更合适形式。:402（animtree risk 标注）是单点 TOOL_META 声明，字面量 detect 最有效。教训：回归守护形式应匹配缺陷形态——单点声明用字面量 detect，分散逻辑用集成测试。
