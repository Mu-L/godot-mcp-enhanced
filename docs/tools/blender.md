# blender

> Blender 程序化建模。action=execute_bpy：AI 写 bpy 片段，headless 跑，自动导 glb 到 res://。（⚠️ bpy 是全功能 Python，威胁面=宿主 RCE，高于 execute_gdscript 沙箱一个量级。bpy 代码经沙箱扫描（已知危险 API 模式，清单不列举，防沙箱边界被侦察）+ 双 opt-in 旁路，对齐 execute_gdscript 哲学；glb 导出落点另有 resolveWithinRoot 约束。本地单用户信任模型。）

| 属性 | 值 |
|------|-----|
| 所属层 | headless |
| 安全级别 | guarded |
| 需要 Godot | 是 |
| 需要编辑器 | 否 |
| 只读 | 否 |
| 长耗时 | 否 |

## Actions

- `execute_bpy`

## Parameters

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `action` | enum (1 项) | ✓ | 操作类型 |
| `project_path` | string |  | Godot 项目目录路径 |
| `export_path` | string | ✓ | 相对项目根的 glb 导出路径，可带可选 res:// 前缀（如 assets/models/rock.glb） |
| `code` | string | ✓ | bpy 建模片段（无需 import/export，godot-mcp 自动包装） |
| `timeout` | number |  | 超时秒数（默认 60） |

## 风险分布

进程 1

---
<!-- AUTO-GENERATED by scripts/gen-tool-docs.mjs from capability-matrix.json v0.32.8. DO NOT edit manually; re-run npm run gen:tool-docs after tool changes. -->
