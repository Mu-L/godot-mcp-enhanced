# analysis

> 理解层静态分析（零 Godot 依赖）。action=signal_map 列全项目信号连接（.tscn [connection] 声明 + .gd 代码 connect/emit 引用，两来源分开标注）；action=impact_check 改动前影响面评估——改信号列出全部连接方/发射方/监听方，改脚本列出引用它的场景与节点，改场景列出其连接/脚本/被实例化处。盲区诚实标注：运行时动态信号名/autoload 间连接不可见。

| 属性 | 值 |
|------|-----|
| 所属层 | headless |
| 安全级别 | safe |
| 需要 Godot | 否 |
| 需要编辑器 | 否 |
| 只读 | 是 |
| 长耗时 | 否 |

## Actions

- `signal_map`
- `impact_check`

## Parameters

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `action` | enum (2 项) | ✓ | signal_map=信号连接全景；impact_check=改动影响面 |
| `project_path` | string | ✓ | 项目路径 |
| `signal` | string |  | 信号名过滤（signal_map 精确匹配；impact_check 必填三选一） |
| `scene` | string |  | signal_map: 场景路径子串过滤（如 scenes/ui） |
| `script_path` | string |  | impact_check: 脚本路径（res:// 或绝对） |
| `scene_path` | string |  | impact_check: 场景路径（res:// 或绝对） |
| `limit` | number |  | 每列表截断上限（默认 200） |

## 风险分布

读 2

---
<!-- AUTO-GENERATED by scripts/gen-tool-docs.mjs from capability-matrix.json v0.32.6. DO NOT edit manually; re-run npm run gen:tool-docs after tool changes. -->
