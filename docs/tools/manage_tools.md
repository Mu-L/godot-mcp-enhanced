# manage_tools

> 动态管理工具组的启用/停用状态。始终可用，不可被禁用。支持 list_groups（列出所有组+profile 价格标签）、discover（关键词发现工具——全量 45 工具×action 搜索评分,返回匹配/所属组/激活态/激活指引,未激活组里的能力由此发现）、activate（启用组）、deactivate（停用组）、sync（同步连接状态）、reconnect（手动重连）。

| 属性 | 值 |
|------|-----|
| 所属层 | headless |
| 安全级别 | danger-api |
| 需要 Godot | 否 |
| 需要编辑器 | 否 |
| 只读 | 是 |
| 长耗时 | 否 |

## Actions

- `list_groups`
- `discover`
- `activate`
- `deactivate`
- `sync`
- `reconnect`
- `migrate`

## Parameters

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `action` | enum (7 项) | ✓ | 操作类型 |
| `groups` | array |  | 目标组名数组（activate/deactivate 时使用） |
| `query` | string |  | discover: 关键词(空格/逗号分词,大小写不敏感)。按工具名/action 名/组名/描述加权评分,返回 top 匹配与激活指引 |
| `top` | number |  | discover: 返回条数上限(默认 8,最大 20) |

## 风险分布

读 5 / 写 2

---
<!-- AUTO-GENERATED by scripts/gen-tool-docs.mjs from capability-matrix.json v0.32.21. DO NOT edit manually; re-run npm run gen:tool-docs after tool changes. -->
