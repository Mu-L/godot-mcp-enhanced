node_modules/
build/
coverage/
*.js.map
*.tgz
*.zip
.DS_Store
Thumbs.db
.env
.idea/
.vscode/
# ZCode ADE 本地会话产物(plan-sess_*),不跟踪
.zcode/
# F2 (review): Cursor 本地 MCP 配置含开发者本地 Godot 路径,不跟踪(用 .cursor/mcp.json.example 作模板)
.cursor/mcp.json
*.swp
*.swo

# Python cache (linting helpers)
.ruff_cache/

# Review reports
.reviews/

# Generated/temporary lists
all_ecc_skills.txt
closed_skills.txt

# Claude Code worktrees
.claude/worktrees/

# TypeScript incremental build info
tsconfig.tsbuildinfo

# E2E test generated files
test/e2e-scene/.godot/
test/e2e-scene/*.png
# M-2: E2E fixture 运行时产物(Godot 编辑器/导入缓存/截图)—— 保留 project.godot 等源文件
test/fixtures/**/.godot/
test/fixtures/input-seq-e2e/mcp_bridge.gd
# real-project 的 mcp_bridge.gd 由 game_bridge_install 拷入、afterAll 删除(不入库),Godot import 生成的伴随 .uid 同为副作用(PR#58 后 e2e 实跑生成)
test/fixtures/real-project/mcp_bridge.gd.uid
# e2e-full-tool-verification write_script/quick_scene 用例的运行时产物(内容确定但每次实跑重新生成;.uid 曾被误提交,已移出索引)
test/fixtures/e2e-project/scenes/e2e_verify_test.tscn
test/fixtures/e2e-project/scripts/e2e_verify_test.gd
test/fixtures/e2e-project/scripts/e2e_verify_test.gd.uid
test/fixtures/input-seq-e2e/NVIDIA Corporation/
test/fixtures/**/*.png
test/fixtures/**/*.png.import
# Task4(screenshot diff): visual/ 是入库的视觉校准素材(非运行时产物),放行
!test/fixtures/visual/*.png
# M3c: gdscript 检查项目运行时复制的 addon(保留 project.godot 源)
test/fixtures/gdscript-check/addons/
# I-3 (P2-4 审查): gdscript 检查项目运行时复制的 src/scripts(check-gdscript.ts 扩编译范围)
test/fixtures/gdscript-check/src/
# A0-3: e2e-resilience-editor 复制的 addon(同 M3c 模式,保留 project.godot 源)
test/fixtures/real-project/addons/
# e2e-asset-tools save 用例落盘产物(pillar.tscn,每次跑覆盖,不清理属测试设计)
test/fixtures/real-project/GeneratedAssets/
.tmp-qa-debug/
.tmp-qa-e2e-reports/
